# 2026-08-05

## Document the c backend and remove the same stray doc comment as cpp

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/c/mod.rs` (1316 lines), the structural twin of `cpp`
reviewed earlier today — both share the `c_family` emission core. Same
findings: `collect_struct_initializers`/`collect_table_initializers` already
delegate to shared `c_family` functions (even more centralized than the
julia-backend fix from earlier), so nothing to factor. Filled the same
missing pair of doc comments (`emit_named_type`/`emit_type_base_and_suffix`,
C's declarator-suffix array placement) as on `cpp`.

Found the identical stray misattributed doc-comment bug found on `cpp`: "Emits
one FIR statement into C syntax." sat above `c_for_loop_step` but actually
describes `emit_stmt` further down, which already has its own complete doc
block. Same likely cause — a leftover from whatever edit introduced (or
copied) this defect between the two structurally parallel backends. Removed
it.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::c` (6 passed); `cargo test -p codegen --lib` (333 passed,
unchanged); `cargo clippy -p codegen --lib` clean.

## Document the cpp backend and remove a stray misattributed doc comment

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/cpp/mod.rs` (1579 lines) for documentation gaps and
factorization opportunities, continuing the sweep. This is the
best-factored backend seen in the sweep so far: `DeclareVar`/`DeclareTable`
and loop-statement handling are already delegated to the shared
`c_family::emit_stmt_common` core, so the "Fields vs Body branch duplicates
the same trailing logic" shape fixed on the `asc` and `cmajor` backends
earlier today simply does not exist here — consistent with this backend's
"stream done" status recorded in project memory. Only two functions lacked
doc comments (`emit_named_type`/`emit_type_base_and_suffix`, which handle
C's declarator-suffix array-size placement); filled both.

Found and fixed one real documentation defect rather than just a gap: a
stray doc-comment line, "Emits one FIR statement using the active rendering
mode.", sat directly above `cpp_for_loop_step` — but that sentence actually
describes `emit_stmt_with_mode` further down, which already carries its own
complete doc block. Almost certainly a leftover from an earlier code move
that left an orphaned line attached to the wrong function. Removed it;
`cpp_for_loop_step` keeps its own correct one-line doc.

No factorization applied — the shared C-family core already does that work,
and the remaining backend-specific fallback bodies in
`emit_dsp_contract_methods` are each genuinely distinct text, not disguised
duplication.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::cpp` (15 passed); `cargo clippy -p codegen --lib` clean.

## Document the asc backend and factor its DeclareVar initializer logic

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/asc/mod.rs` (1698 lines) for documentation gaps and
factorization opportunities, continuing the sweep. Somewhat less polished
than the backends reviewed so far — ten undocumented functions instead of
the usual zero to two — but nothing structurally wrong. Filled the genuine
gaps (`decode_module`, `is_dsp_api_method`, `backend_id`, `invalid_section`,
`unsupported_node`, `format_array`), matching the documentation level their
counterparts already carry in the other backends covered by this sweep.
Left the trivial one-liners (`faust_float_type`, `math_namespace`) alone,
and left the `block_declares_var`/`block_stores_var` pair alone for the same
reason as the equivalent pair found on the Julia backend: real but too
marginal (5 lines, single call site) to justify the indirection.

Found and applied one genuine factorization: `emit_stmt`'s `DeclareVar` arm
had two branches (`Phase::Fields` field declarations vs. function-body
locals) that only differ in how the declaration head renders (`name: type`
member vs `let name: type` local); the twelve-line "write the explicit FIR
initializer, or default-initialize a non-empty array with
`new StaticArray<T>(size)`" logic that follows was duplicated verbatim in
both branches. Extracted into `emit_declare_var_init`.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::asc` (8 passed, including the array-default-initialization test
exercising the refactored path); `cargo test -p codegen --lib` (333 passed,
unchanged); `cargo clippy -p codegen --lib` clean.

## Document the cmajor backend and factor its static-qualifier logic

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/cmajor/mod.rs` (1805 lines) for documentation gaps and
factorization opportunities, continuing the sweep. Documentation was already
essentially complete (the one undocumented function, a trivial `has_bargraphs`
getter, wasn't worth annotating). The backend already factors its lifecycle
emission well — `emit_lifecycle` calls a shared `emit_named_body` helper four
times rather than repeating each method's body.

Found and applied one small genuine duplication: the `DeclareVar` and
`DeclareTable` arms of `emit_stmt` each recomputed the same `const `
storage-qualifier logic verbatim (whether `AccessType::Static` renders as
`const`, which does not apply in field position since Cmajor has no
class-static storage). Extracted into a shared `static_qualifier` helper.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::cmajor` (6 passed); `cargo test -p compiler --test cmajor_backend`
(17 passed, including the field/static-declaration coverage this change
touches); `cargo clippy -p codegen --lib` clean.

## Document the julia backend and factor its initializer collectors

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/julia/mod.rs` (1889 lines) for documentation gaps and
factorization opportunities, continuing the sweep. Documentation was already
essentially complete — the one function without a doc comment (`julia_name`,
a trivial getter) wasn't worth adding noise for. `emit_binop`/`emit_binop_expr`
looked at first glance like the kind of duplication fixed on the Cranelift
and Rust backends, but turned out to already be a clean split (a generic
token table plus two operators needing non-infix rendering), cross-
referenced in both docs.

Found and applied one genuine factorization: `collect_struct_initializers`
and `collect_table_initializers` both walked `[dsp_struct, globals]`,
validated each section as a FIR `Block`, and flattened its items, differing
only in which `FirMatch` variant they filtered for afterward. Extracted the
shared walk into `struct_and_global_items`; both collectors now just
filter-map over it. Left the smaller, single-use `block_declares_var`/
`block_stores_var` pair alone — same shape but 5 lines each, not enough
duplication to justify the added indirection.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::julia` (7 passed); `cargo test -p compiler --lib` (67 passed);
`cargo test -p compiler --test signal_fir_lane` (2 Julia-relevant tests
passed); `cargo clippy -p codegen --lib` clean.

## Document the codebox backend

Commit date: 2026-08-05

Source commit: this commit

Reviewed both source files of the codebox (RNBO `codebox~`) backend —
`mod.rs` (the FIR-to-codebox emitter) and `eval.rs` (the small codebox
evaluator used to numerically validate emitted output, since byte parity
with the C++ backend isn't achievable or a goal for this target) — for
documentation gaps and factorization opportunities, continuing the sweep.
This backend was, if anything, even more thoroughly commented than the
others so far: nearly every non-obvious choice already carries a "why"
(deliberate C++ divergences, RNBO wiring specifics, batching semantics).
Actively looked for factorable duplication this time given the Rust backend
findings, but every apparent table (`map_math_name`, `emit_binop`,
`eval::builtin`) turned out to be a legitimate lookup table rather than
disguised duplication — nothing to factor here.

Filled two minor gaps: `widget_prefix_slider` in `mod.rs` was missing the
doc comment its sibling `widget_prefix_button` carries; `exec` and `eval` in
`eval.rs`, the evaluator's two central dispatch functions, got doc comments
matching the standard already applied to the equivalent `lower_stmt`/
`lower_expr` functions elsewhere in this crate.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::codebox` (7 passed); `cargo test -p compiler --test
codebox_backend` (40 passed); `cargo clippy -p codegen --lib` clean.

## Factor out duplicated boilerplate in the Rust backend emitter

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/rust/mod.rs` for documentation gaps as the next step of the
doc-only sweep and found it already fully documented, with no gaps to fill —
the first backend in this sweep where that was true. Asked to look for
factorization value instead, and found two genuine cases of mechanical
duplication (as opposed to the rest of the file, where each match arm carries
distinct emission logic and isn't a good factoring candidate).

`emit_faust_dsp_trait_impl` emitted 15 near-identical `writeln!` calls, each
forwarding one `FaustDsp` trait method to its identically-named inherent
method with a fixed signature. Replaced with a `&[&str]` table of the 15
forwarding declarations and a loop, mirroring the table-driven style already
used for the And/Or/Xor operator tokens just below in the same file.

`emit_binop_expr`'s float-path `Add`/`Sub`/`Mul`/`Div`/`Rem` arms were five
near-identical arms differing only by operator token; merged into one arm
with a token lookup, the same shape as the And/Or/Xor arm right below it and
the same pattern used to merge Cranelift's `lower_binop` float arms earlier
in this sweep. Net change: -50 lines, no behavior change.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib`
(333 passed, unchanged from before — including
`lifecycle_conformance_matches_cpp_reference_order` and
`integer_state_arithmetic_uses_wrapping_semantics`, which would catch any
emitted-text regression from either refactor); `cargo clippy -p codegen
--lib` clean.

## Document the wasm backend

Commit date: 2026-08-05

Source commit: this commit

Reviewed the two non-test source files of the WASM backend (`layout.rs`,
`mod.rs`; ~5,500 lines) for documentation gaps, continuing the doc-only sweep
from Cranelift and interp. Already thoroughly documented overall; filled five
targeted gaps: why `Stack`-declared array locals need a linear-memory offset
in `layout.rs` (WASM's native `local` slots are scalar-only, unlike a native
stack); the four `SOUNDFILE_*_OFFSET` constants in `mod.rs`, which encode the
`Soundfile` struct at WASM's 4-byte pointer spacing versus the 8-byte spacing
used by native 64-bit backends for the same struct; a missing doc comment on
the recursive `lower_switch_cases` if/else-chain emitter; why `count` and
`sample_rate` safely share WASM local index 1 (they never coexist in the same
function body); and a step-by-step comment on `LoadSoundfileBuffer` lowering,
mirroring the already-commented Cranelift equivalent for a computation that's
harder to follow on WASM's implicit operand stack than with named SSA values.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::wasm` (38 passed); `cargo clippy -p codegen --lib` clean.

## Add small-format `.fbc` read support to the interpreter serializer

Commit date: 2026-08-05

Source commit: this commit

`write_fbc` has always supported two `.fbc` text formats (normal and compact
"small"), matching the C++ interpreter and the original porting plan
(`porting/phases/faust-rust-interpreter-porting-en.md`: "Two modes exist in
C++: normal and small. The Rust port must support both."), but `read_fbc`
only ever implemented the normal-format grammar. Small-format output was
unreadable by the Rust reader; every production `write_fbc` call site already
passed `small=false`, so the gap was latent rather than user-visible.

Auditing the reader showed its block/instruction-level parsing (meta, UI,
code blocks) never validates header keywords by literal text — it discards
label tokens positionally — so the two formats already aligned token-for-token
there. Only the top-level file header (7 lines, previously gated by
`check_token` against normal-mode-only keywords) and one literal `label ==
"name"` check on the optional per-instruction name field needed a small-mode
branch. `read_fbc` now detects the format from the first header token
(`"interpreter_dsp_factory"` vs `"i"`) and reads the matching header grammar;
the name-field check became positional so it accepts both `"name"` and `"n"`.

Confirmed `interp-ffi::read_fbc_any`'s precision auto-detection (which only
checks whether the header line ends in `"double"`) already worked unmodified
for both formats, and that no `.fbc` fixtures in the tree depend on the
small format.

Validation: `cargo build -p codegen -p interp-ffi --lib`; `cargo test -p
codegen --lib` (333 passed, up from 331 — two new tests: a full small-format
write/read round trip, and a `name`-field round trip exercised in both
formats, previously untested in either); `cargo clippy -p codegen --lib`
clean.

## Document the interp backend

Commit date: 2026-08-05

Source commit: this commit

Reviewed all 13 non-test source files of the interpreter (`interp`) backend
(~12,700 lines: `bytecode.rs`, `compiler.rs`, `executor.rs`, `factory.rs`,
`fbc_to_cpp.rs`, `foreign.rs`, `instance.rs`, `mod.rs`, `opcode.rs`,
`optimizer.rs`, `real.rs`, `serial.rs`, `soundfile.rs`) for documentation
gaps, continuing the doc-only sweep started on the Cranelift backend. This
backend turned out to be the most thoroughly documented in the crate: every
public item and most private helpers already carry a doc comment with precise
C++ source provenance, including exact line numbers in several places
(`instance.rs`). 11 of 13 files needed no changes.

Added one genuine gap fill: `foreign::is_supported_signature`'s shape
restriction (0-2 args, homogeneous type, matching the return type unless
`Void`) previously had no rationale attached. It mirrors the finite,
hand-enumerated set of `extern "C" fn` signatures that `foreign_call::invoke`
dispatches to via `transmute` in a separate crate — there is no generic FFI
path, so any signature outside that fixed table cannot actually be invoked at
runtime regardless of what the interpreter compiler accepts.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::interp` (164 passed); `cargo clippy -p codegen --lib` clean.

## Add Cranelift math-symbol coverage tests and polish backend documentation

Commit date: 2026-08-05

Source commit: this commit

Added exhaustive white-box unit tests for the Cranelift backend's four pure
`FirMathOp` -> host-symbol lookup tables (`unary_math_symbol_f32/f64`,
`binary_math_symbol_f32/f64`) in `lowering.rs`. The test's expected-value
match has no wildcard arm, so adding a new `FirMathOp` variant fails to
compile here instead of silently falling through the `_ => None` arms in the
production tables; the guarantee was confirmed by temporarily adding a
fictitious variant and reverting.

Reviewed all non-test source files in the Cranelift backend (`api.rs`,
`core.rs`, `host.rs`, `jit_data.rs`, `subset.rs`, `lowering.rs`) for
documentation gaps. The backend was already extensively documented; filled
the handful of genuine gaps found: `find_module_and_compute` and
`fir_type_to_clif_type` in `core.rs`; the non-obvious `exp10`/`abs`/`rint`/
`remainder` host wrapper semantics in `host.rs`; the generic
`function_body_subset_gap_reason_from_decl` engine in `subset.rs`; and the
silent element-type-mismatch behavior of static-table serialization in
`jit_data.rs`.

Merged the two near-identical `FirType::Float32 | FirType::FaustFloat` and
`FirType::Float64` match arms in `ComputeLowering::lower_binop` into one,
since Cranelift's `fadd`/`fsub`/`fmul`/`fdiv`/`fcmp` are width-agnostic and
operate on whatever CLIF value type the already-coerced operands carry. Pure
refactor, no behavior change.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::cranelift` (32 passed); `cargo clippy -p codegen --lib` clean for
the touched module.
