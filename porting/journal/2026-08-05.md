# 2026-08-05

## Flatten generator sub-modules for non-nesting backends (S3)

Commit date: 2026-08-05

Source commit: this commit

`fir::subcontainer::flatten_sub_modules` inlines a table generator into the
program that calls it, replacing the allocate/init/fill triple with the
sub-module's own bodies. This is the port of the C++
`inlineSubcontainersFunCalls` + `DspRenamer` + `mergeSubContainers` trio, which
upstream applies for the backends that cannot express a nested container. Two
state policies: `StackLocals` demotes the generator's state to locals of the
initialization function (what a target with a `static classInit` needs, since a
static method has no instance to hold fields), `MergedStructFields` keeps it in
the DSP struct under a prefixed name (what `interp` and `wasm` need, their
`classInit` being instance-scoped). Names are prefixed either way: a sub-module
is lowered with its own fresh counters, so its `iRec0` has nothing to do with
the enclosing program's.

The pass reuses the inliner's hygienic clone engine instead of duplicating a
50-arm traversal. Two small generalizations were needed there:
`fun_arg_subst` went from `name -> name` to `name -> (name, AccessType)`, and a
`struct_subst` map was added. The reason is specific: `prepare_callee_body_for_inlining`
materializes every parameter into a stack temp, but a flattened `fill` must
write into the caller's *own* table, which lives in `Static` or `Struct`
storage — so the access class has to travel with the substituted name rather
than being assumed.

The tests run the real S2 producer and flatten its output rather than
hand-building a module. That mattered immediately: `verify_flattened` — written
as an independent check, not as a mirror of the pass — failed on the nested
fixture with "allocation of 'mydspSIG0SIG0' survived flattening". Two causes,
both mine. First, I resolved which sub-module a call belonged to via its
receiver object, but the clone engine hoists `NewDsp` out of its declaration
into a standalone statement, so the receiver map was never populated for the
nested case; resolving from the callee name instead is stable under cloning.
Second, splicing a sub-module's `instanceInit` can introduce a *new*
allocate/init/fill triple, for a generator nested one level deeper, so the
inlined statements have to go through the rewrite again.

S3 also exposed a defect in the S2 producer that the S2 tests had not: fill
bodies carried the DSP path's `output0 = outputs[0]` channel aliases, which
reference an `outputs` parameter that a `fill(dsp, count, table)` signature
does not have. It was invisible until flattening spliced those statements into
a function where the checker could see them. Fixed by not emitting the aliases
when lowering a fill module.

## Compile table generators into sub-modules (S2 producer)

Commit date: 2026-08-05

Source commit: this commit

The second half of S2: `--table-init runtime` now compiles every `SIGGEN`
payload into its own FIR sub-module whose `fill` computes the table content at
initialization time. `const` stays the effective default until S7, so nothing
observable changes for existing users yet.

The milestone is coverage, not size. The three programs that previously could
not be compiled at all — `subcontainer1.dsp` (`ma.SR` inside the generator), a
sample-rate-dependent `exp(-t/SR)` table, and an `ffunction` generator — now
lower successfully and stop at the S1 backend guard with `FRS-CGEN-CPP-0003`
instead of `FRS-SFIR-0004`. Those values do not exist at compile time; the only
way to express them is to compute them at init, which is what the sub-module
does.

Shape checks on the S0 fixtures: one sub-module per generator, one for two
reads of the same generator tree (`f11`), `mydspSIG0`/`mydspSIG1` for two
distinct ones (`f12`), three for the mixed-type probe (`f13`), and
`mydspSIG0SIG0` nested inside `mydspSIG0` for `f08` — matching the upstream
naming convention, nesting suffix included.

Mechanically the generator goes through the same `build_module` as the main
program, with an output sink redirecting the single output to `table[i0]` at the
sub-module's element type and no `FaustFloat` cast (C++ `compileSingleSignal`
pushes exactly that store). Recursion carriers, delay strategies, CSE and
scheduling are reused untouched. The result is lowered into its own `FirStore`
and imported through the `FirStore::import_from` added in S1. The parent emits
the C++ sequence — allocate the sub-container object as a stack local,
`instanceInit`, `fill` — with deallocation left to the backends, since it is
bound to how each language allocates and upstream itself skips it for Rust,
Julia and AssemblyScript.

Two bugs are worth recording; both were silent-wrong-answer shaped.

First, `assemble_sub_module` read its nested sub-modules from the caller's
`FillSpec`, which is always empty at that point, instead of from the recursive
lowering's own accumulator. Nested generators were dropped entirely.

The second is the instructive one. A sub-module's `instanceInit` was assembled
from constants + reset + clear, mirroring C++ `generateInstanceInitFun`. But a
sub-module has no `classInit`, so the fill call for a *nested* generated table —
which the recursive lowering had correctly placed in `static_init_statements` —
had nowhere to go and vanished. That yields an inner table declared and never
written, and an outer table computed from zeros: precisely the upstream 2.87.1
defect that §2.4 of the plan documents and that contract C5 exists to avoid. I
wrote the plan warning about this defect and then reproduced it anyway, which is
a fair argument for writing the checker before trusting the producer. Fixed by
prepending `static_init_statements` to the sub-module's `instanceInit`, and
locked by a new rule FIR-SM05 — verified by reintroducing the bug and watching
it fire:

```
error: sub-module 'mydspSIG0' owns nested generator 'mydspSIG0SIG0' but never
calls 'fillmydspSIG0SIG0' from 'instanceInitmydspSIG0'; the nested table would
be read uninitialized
```

FIR-SM01 also had to be reformulated. As first written it flagged any
uninitialized array read by `compute` with no fill call, and fired immediately
on ordinary delay lines (`iVec6`) — also uninitialized arrays, but zeroed by
`instanceClear` rather than filled. It now keys on the sub-module (every
declared sub-module must have its fill called): precise, producer-independent,
and still failing on the drop-the-call mutation.

## Adopt upstream table naming (S2 rename step)

Commit date: 2026-08-05

Source commit: `c093e424`

Naming-only step at the head of S2, deliberately separate from the semantic
change that follows so a later bisect can tell "renamed" from "changed".
Literal waveform tables become `{i|f}{module}Wave{j}` with a matching
`{name}_idx` cycling field; generated tables become `{i|f}tbl{k}`. Both counters
are allocation-ordered and shared across element types, matching C++
`getTypedNames`, where the `i`/`f` letter is a prefix rather than part of the
counter key.

Waveform output is now identical to the reference compiler — `fmydspWave0[3]`,
`imydspWave1[3]`, `fmydspWave0_idx` — the first place where the two compilers
agree character for character.

Generated tables match in form but not always in counter order: on
`f13_mixed_type_tables` upstream allocates `itbl0`(64), `ftbl1`(32), `itbl2`(16)
in program order while faust-rs allocates `ftbl0`(32), `itbl1`(64), `itbl2`(16).
Both compilers order the counter by allocation; what differs is when each one
first materializes a table during lowering. Stable across runs, no semantic
effect, recorded in plan §5.6 as a residual for S4a to note rather than a defect
to fix — closing it means aligning traversal order, a separate concern.

## Add the FIR sub-module node and its checker rules (S1)

Commit date: 2026-08-05

Source commit: this commit

S1 of the sub-module table-init plan: the FIR model now has somewhere to put a
table generator, and a checker that refuses the shapes which would silently
produce a zero-filled table. No producer yet — `faust-rs` emits byte-identical
output to the S0 baseline (`f01_osc_table` still 1 420 248 bytes), which is the
intended end state for this phase.

Added `FirMatch::SubModule` (tag `FIRST_SUBMODULE`) carrying name, element type,
its own state/static/global/function blocks, and nested sub-modules; `Module`
gains an eighth field `sub_modules`. `FirStore::import_from` wraps the existing
`TreeArena::clone_subtree_from`, which is what S2 needs to graft a generator
lowered in its own store into the parent one.

Four checker rules, each with a fixture that must fail when the rule is
disabled — verified by actually disabling them, not by assuming:

- SM01: a table declared without an initializer and read by `compute`, with no
  `fill` call in `staticInit`/`instanceConstants`. This is the rule that makes
  the upstream nesting defect impossible here.
- SM02: sub-module shape — exactly `instanceInit{name}` and `fill{name}`, all
  sections blocks, nested sub-modules recursively checked.
- SM03: `fill` signature (`count: Int32`, `table: Ptr(elem)`) and a body that
  writes only through its own `table` argument.
- SM04: sub-module name uniqueness across the whole nesting tree.

Two things went wrong and are worth recording.

The rule codes were first written as FIR-T01…T04, following the plan. They
collide: `FIR-T01`–`T04` are already the table-access rules (index type, store
element type, missing declaration) and `FIR-M06` is also taken. The mutation
tests still passed while the collision was live — they were matching a
pre-existing diagnostic, not the new one. Renamed to `FIR-SM01`…`SM04`, and the
plan updated to match. The lesson is the one the plan already states about
independent checks: a test that asserts on a shared identifier proves less than
it appears to.

`FirBuilder::module` takes `sub_modules: &[FirId]`, not a pre-built block, so
the ~100 existing call sites just append `&[]`. But the clone paths in
`fir::inliner` must clone the block's *items*, not the block node — passing the
cloned block would nest a block inside a block and break every consumer that
expects `sub_modules` to decode as a block of `SubModule`. Added
`clone_sub_modules` for that, after writing the bug first.

Every backend decoder now fails closed on a non-empty `sub_modules`, with its
own stable error code (`Unsupported`/`UnsupportedNode`/`CompilationFailed`
depending on what each enum already had) and a shared message pointing at
`--table-init const`. Three backends — cmajor, codebox, wasm — destructured
`FirMatch::Module` with `..` and would otherwise have compiled happily while
ignoring the field, emitting a table declaration that nothing fills. That is
the silent-wrong-answer failure this phase exists to prevent, so it gets an
explicit guard and a backend test rather than trust.

## Freeze the S0 baseline for SIGGEN table initialization

Commit date: 2026-08-05

Source commit: this commit

Ran S0 of the sub-module table-init plan: no code change, only the "before"
state frozen under `porting/generated/siggen-table-init-s0/` — thirteen
fixtures, their `faust 2.87.1` output in `ref/` (kept in full, it is the S4a
emission oracle), the current `faust-rs 0cb97dd7` output in `rs/`, and a
baseline document holding sizes, timings, structural counts and the impulse
measurement.

Three results changed the plan rather than just confirming it.

The impulse baseline is **93/93 on cpp, c and interp** — not the cpp 92/93,
c 87/93, interp 74/93 quoted by earlier porting documents and by the project
memory. Those figures are stale; nothing is failing today. The `93`
denominator survived only by coincidence: the corpus grew from 93 to 133
fixtures while 39 clock-domain DSPs (`ondemand_*`, `upsampling_*`,
`downsampling_*`, which upstream rejects with `undefined symbol : ondemand`
and which therefore have no C++ oracle) and `subcontainer1` were excluded,
landing back on 93 gated cases. Plan §8.2 layer 4 is restated from
"no regression against 92/93" to something exact: remove the
`KNOWN_FAIL_all := subcontainer1` line from `known.mk`, take the gate from 93
to 94 cases, and keep all three backends at 100%. There are no pre-existing
failures for a regression to hide behind.

Getting that number required stepping over the false-green trap already
recorded in memory. The first run of `make cpp && make c && make interp`
returned exit 0 in seconds with `Nothing to be done` — the `ir/` trees were
cached from an earlier session. The real measurement needed `ir/{cpp,c,interp}`
and `build/{cpp,c,interp}` removed first, keeping the expensive C++ oracle
cache in `reference/` and `build/ref/`. The evidence recorded is the surviving
`.ir` count rather than the exit code, since `Make.gcc`/`Make.interp` wrap each
comparison as `filesCompare … || (rm -f <ir>; false)`: 93 files means 93
comparisons passed.

Added a thirteenth fixture beyond the plan's twelve. §5.6 rule 1 — one `tbl`
counter shared by integer and real tables — had been inferred from
`getTypedNames` (`instructions_compiler.cpp:1040`) plus a same-type example,
which is not evidence. `f13_mixed_type_tables` produces `itbl0mydspSIG0`,
`ftbl1mydspSIG1`, `itbl2mydspSIG2`: the counter does advance across the type
change, so the `i`/`f` letter is a prefix and not part of the counter key.
Current output for the same program is `fTbl60`, `iTbl108`, `iTbl112` —
`SigId`-derived and unordered, the shape the S2 rename replaces.

Quantified the upstream nesting defect of plan §2.4 on `f08`: the reference
declares `static float ftbl0mydspSIG0SIG0[64]` and `static float
ftbl1mydspSIG0[64]` but emits only **one** filler class, and `classInit` fills
only the second. The inner table stays zero. faust-rs folds the same program
correctly to `{0.0f, 0.5f, 1.0f, 1.5f, …}`. `f08` is therefore a regression
guard, not a parity target — copying upstream there would copy a bug.

Sizes and timings re-measured slightly differently from the plan's figures
(330× rather than 333× on `os.osc`, 124.6× rather than 127× on the constant
table); the drift is the output path appearing in the metadata block. The
baseline document carries the re-measured values. Three `rs/` outputs exceed
32 KB and are stored head+tail with their full byte count inline — the elided
part is the folded literal list, whose size is the measurement.

## Specify SIGGEN table initialization through generated sub-modules

Commit date: 2026-08-05

Source commit: this commit

Asked to analyze replacing the compile-time table-generator interpreter
(`crates/transform/src/signal_fir/siggen.rs`) by the C++ reference mechanism:
compile the generator signal into its own sub-module whose `fill` function
computes the table content at initialization time (`signal2Container` /
`generateSigGen` / `generateStaticTable` upstream).

Probing the two compilers turned up more than the expected code-size argument.
`os.osc(440)` emits 4 261 bytes with `faust 2.87.1` and 1 420 238 with
`faust-rs 2cffc4a3` — a 333× ratio, because the 65536-entry table is folded
into a literal initializer list; the `rwtable` case is 22× and additionally
stores the content twice (static companion array plus per-instance copy). But
the decisive finding is functional: sample-rate-dependent and `ffunction`
generators cannot be folded at all and are rejected with `FRS-SFIR-0004`,
which is exactly what `tests/impulse-tests/dsp/subcontainer1.dsp` — the
canonical upstream test for this feature, and the impulse corpus's one
remaining transform error — exercises. UI widgets in table content are not a
gap: upstream's own type checker rejects them (`checkInit failed`), so neither
compiler has to model them.

One place where the current path is *better* than the reference: for a nested
generated table, upstream 2.87.1 declares the inner table
(`static float ftbl0mydspSIG0SIG0[64]`) but never emits its sub-container and
never fills it, leaving it zero. The interpreter folds the nested case
correctly today. The port must therefore be recursive rather than a literal
transcription of the C++ control flow, and that is written into the plan as a
tested requirement rather than left as an accident to be rediscovered.

Wrote `porting/siggen-subcontainer-table-init-port-plan-2026-08-05-en.md`:
baseline and evidence, the reference model with its two emission shapes
(nested container for the textual backends, `inlineSubcontainersFunCalls` +
`mergeSubContainers` for the flat ones), the FIR gap analysis, the target
design, invariants with their independent checkers and rejecting mutations,
phases S0-S7, the test plan, and risks. The FIR work is a `SubModule` node
plus an eighth `Module` field, a `staticInit` lifecycle function (the interp
backend already reserves the slot, `interp/mod.rs:320`), and sized
uninitialized tables expressed as `DeclareVar(Array)` — no new node kind
needed there, and `TreeArena::clone_subtree_from` already exists for importing
a child store's root.

Four decisions settled with the user over the session, each propagated through
the document rather than appended to it:

1. `cpp` and `c` emit the real nested class/struct, not the simpler
   `StackLocals` flattening. A `static classInit` stays valid because the
   sub-module object is a local of that function. §5.9.1 freezes the target
   text for both languages from reference probes, so the emitter is written
   against an oracle instead of toward one.
2. `--table-init const` is a permanent mode, not a transition scaffold. That
   keeps `siggen.rs`, `register_constant_table_init` and the 256-element
   unroll threshold as supported code — an earlier draft had them scheduled
   for deletion — and a mode matrix gates both paths on every migrated
   backend.
3. Generated tables take the upstream names: `{i|f}tbl{k}{Sub}` for read-only
   tables, `{i|f}tbl{k}` for writable ones (the suffix comes from
   `generateStaticTable`'s `vname += tablename` and is absent in
   `generateTable`), with one allocation-ordered counter shared by int and
   real tables. This lands as a standalone rename commit at the head of S2 so
   the naming diff never mixes with the semantic one, and it strengthens the
   emission-determinism invariant, since `k` is no longer `SigId`-derived.
4. No folded fast path in `runtime` mode: every `SIGGEN` becomes a sub-module,
   waveform and constant payloads included.

Decision 4 corrected a claim I had written into the first draft — that folding
constant and waveform generators produces "strictly smaller" code. Measuring
it disproved that: a constant generator with N = 65536 folds to 395 486 bytes
against the reference's 3 123 (127×, because `size` identical literals are
emitted where upstream emits `table[i1] = 0.5f;` in a loop), and a 5-value
waveform filling a 4096-entry table folds to 30 942 against 3 346. Folding
only wins when table length equals waveform length (2 178 against 3 083). The
subtler argument is that partial folding would skip upstream's counter
increments and renumber every later table in the same DSP, giving back exactly
the reference-diffability that decision 3 was taken to buy.

No code changed. Next step is S0: freeze reference and current outputs for the
plan's twelve fixtures under `porting/generated/`, and re-measure the impulse
baselines (cpp 92/93, c 87/93, interp 74/93 come from earlier documents and
should not be trusted as a starting point without re-running them).

## Record the precedence-aware-printing cross-backend analysis

Commit date: 2026-08-05

Source commit: this commit

While reviewing `cpp`'s already-shared `c_family` statement emission during
today's documentation sweep, asked whether other backends had a bigger
factoring opportunity than the small in-file helpers extracted so far.
Tracing what `c_family` shares — and what its own module doc says upstream's
`Text.hh` shares across every textual backend — led to
`crates/codegen/src/backends/textual.rs`: a faithful port of the C++
reference's precedence-aware infix printing (`TextInstVisitor::
{left,right}ArgNeedsParentheses`), explicitly written to be reusable, but
with exactly one caller (`cmajor`). `c_family` (shared by `c`/`cpp`), `rust`,
`julia`, `asc`, and `codebox` each still always fully parenthesize every
binary expression — the same defect class already found and fixed for
Cmajor, where it produced 111 levels of nesting on `bells.dsp` and made the
Cmajor compiler reject the output outright (fixed 2026-08-04, depth dropped
to 3).

Started a proof-of-concept port on `rust` per the user's choice, but stopped
before writing code: Rust routes integer arithmetic through `wrapping_*`
method calls rather than infix operators (for C-style overflow wrapping),
which has the same unbounded-nesting symptom but isn't fixable by
`textual.rs`'s precedence algorithm — the actual fix there is recognizing an
already-valid Rust postfix/method-call chain, a different problem. Rust also
interleaves explicit `as` casts into operand rendering before combining
operands, which changes what a precedence-aware rewrite needs to inspect.

Recorded the full analysis — evidence table, the Cmajor reference shape to
mirror, the Rust complication, and open questions about whether any
non-Cmajor backend has an active (not just latent) failure — in
`porting/textual-precedence-aware-printing-cross-backend-analysis-2026-08-05-en.md`,
and filed the scoped follow-up as task `task_37b2ce9f` rather than pushing
the `rust` change through incompletely.

## Document the c backend and remove the same stray doc comment as cpp

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/c/mod.rs` (1316 lines), the structural twin of `cpp`
reviewed earlier today — both share the `c_family` emission core. Same
findings: `collect_struct_initializers`/`collect_table_initializers` already
delegate to shared `c_family` functions (even more centralized than the
julia-backend fix from earlier), so nothing to factor. Filled the same
missing pair of doc comments (`emit_named_type`/`emit_type_base_and_suffix`,
C's declarator-suffix array placement) as on `cpp`.

Found the identical stray misattributed doc-comment bug found on `cpp`: "Emits
one FIR statement into C syntax." sat above `c_for_loop_step` but actually
describes `emit_stmt` further down, which already has its own complete doc
block. Same likely cause — a leftover from whatever edit introduced (or
copied) this defect between the two structurally parallel backends. Removed
it.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::c` (6 passed); `cargo test -p codegen --lib` (333 passed,
unchanged); `cargo clippy -p codegen --lib` clean.

## Document the cpp backend and remove a stray misattributed doc comment

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/cpp/mod.rs` (1579 lines) for documentation gaps and
factorization opportunities, continuing the sweep. This is the
best-factored backend seen in the sweep so far: `DeclareVar`/`DeclareTable`
and loop-statement handling are already delegated to the shared
`c_family::emit_stmt_common` core, so the "Fields vs Body branch duplicates
the same trailing logic" shape fixed on the `asc` and `cmajor` backends
earlier today simply does not exist here — consistent with this backend's
"stream done" status recorded in project memory. Only two functions lacked
doc comments (`emit_named_type`/`emit_type_base_and_suffix`, which handle
C's declarator-suffix array-size placement); filled both.

Found and fixed one real documentation defect rather than just a gap: a
stray doc-comment line, "Emits one FIR statement using the active rendering
mode.", sat directly above `cpp_for_loop_step` — but that sentence actually
describes `emit_stmt_with_mode` further down, which already carries its own
complete doc block. Almost certainly a leftover from an earlier code move
that left an orphaned line attached to the wrong function. Removed it;
`cpp_for_loop_step` keeps its own correct one-line doc.

No factorization applied — the shared C-family core already does that work,
and the remaining backend-specific fallback bodies in
`emit_dsp_contract_methods` are each genuinely distinct text, not disguised
duplication.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::cpp` (15 passed); `cargo clippy -p codegen --lib` clean.

## Document the asc backend and factor its DeclareVar initializer logic

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/asc/mod.rs` (1698 lines) for documentation gaps and
factorization opportunities, continuing the sweep. Somewhat less polished
than the backends reviewed so far — ten undocumented functions instead of
the usual zero to two — but nothing structurally wrong. Filled the genuine
gaps (`decode_module`, `is_dsp_api_method`, `backend_id`, `invalid_section`,
`unsupported_node`, `format_array`), matching the documentation level their
counterparts already carry in the other backends covered by this sweep.
Left the trivial one-liners (`faust_float_type`, `math_namespace`) alone,
and left the `block_declares_var`/`block_stores_var` pair alone for the same
reason as the equivalent pair found on the Julia backend: real but too
marginal (5 lines, single call site) to justify the indirection.

Found and applied one genuine factorization: `emit_stmt`'s `DeclareVar` arm
had two branches (`Phase::Fields` field declarations vs. function-body
locals) that only differ in how the declaration head renders (`name: type`
member vs `let name: type` local); the twelve-line "write the explicit FIR
initializer, or default-initialize a non-empty array with
`new StaticArray<T>(size)`" logic that follows was duplicated verbatim in
both branches. Extracted into `emit_declare_var_init`.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::asc` (8 passed, including the array-default-initialization test
exercising the refactored path); `cargo test -p codegen --lib` (333 passed,
unchanged); `cargo clippy -p codegen --lib` clean.

## Document the cmajor backend and factor its static-qualifier logic

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/cmajor/mod.rs` (1805 lines) for documentation gaps and
factorization opportunities, continuing the sweep. Documentation was already
essentially complete (the one undocumented function, a trivial `has_bargraphs`
getter, wasn't worth annotating). The backend already factors its lifecycle
emission well — `emit_lifecycle` calls a shared `emit_named_body` helper four
times rather than repeating each method's body.

Found and applied one small genuine duplication: the `DeclareVar` and
`DeclareTable` arms of `emit_stmt` each recomputed the same `const `
storage-qualifier logic verbatim (whether `AccessType::Static` renders as
`const`, which does not apply in field position since Cmajor has no
class-static storage). Extracted into a shared `static_qualifier` helper.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::cmajor` (6 passed); `cargo test -p compiler --test cmajor_backend`
(17 passed, including the field/static-declaration coverage this change
touches); `cargo clippy -p codegen --lib` clean.

## Document the julia backend and factor its initializer collectors

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/julia/mod.rs` (1889 lines) for documentation gaps and
factorization opportunities, continuing the sweep. Documentation was already
essentially complete — the one function without a doc comment (`julia_name`,
a trivial getter) wasn't worth adding noise for. `emit_binop`/`emit_binop_expr`
looked at first glance like the kind of duplication fixed on the Cranelift
and Rust backends, but turned out to already be a clean split (a generic
token table plus two operators needing non-infix rendering), cross-
referenced in both docs.

Found and applied one genuine factorization: `collect_struct_initializers`
and `collect_table_initializers` both walked `[dsp_struct, globals]`,
validated each section as a FIR `Block`, and flattened its items, differing
only in which `FirMatch` variant they filtered for afterward. Extracted the
shared walk into `struct_and_global_items`; both collectors now just
filter-map over it. Left the smaller, single-use `block_declares_var`/
`block_stores_var` pair alone — same shape but 5 lines each, not enough
duplication to justify the added indirection.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::julia` (7 passed); `cargo test -p compiler --lib` (67 passed);
`cargo test -p compiler --test signal_fir_lane` (2 Julia-relevant tests
passed); `cargo clippy -p codegen --lib` clean.

## Document the codebox backend

Commit date: 2026-08-05

Source commit: this commit

Reviewed both source files of the codebox (RNBO `codebox~`) backend —
`mod.rs` (the FIR-to-codebox emitter) and `eval.rs` (the small codebox
evaluator used to numerically validate emitted output, since byte parity
with the C++ backend isn't achievable or a goal for this target) — for
documentation gaps and factorization opportunities, continuing the sweep.
This backend was, if anything, even more thoroughly commented than the
others so far: nearly every non-obvious choice already carries a "why"
(deliberate C++ divergences, RNBO wiring specifics, batching semantics).
Actively looked for factorable duplication this time given the Rust backend
findings, but every apparent table (`map_math_name`, `emit_binop`,
`eval::builtin`) turned out to be a legitimate lookup table rather than
disguised duplication — nothing to factor here.

Filled two minor gaps: `widget_prefix_slider` in `mod.rs` was missing the
doc comment its sibling `widget_prefix_button` carries; `exec` and `eval` in
`eval.rs`, the evaluator's two central dispatch functions, got doc comments
matching the standard already applied to the equivalent `lower_stmt`/
`lower_expr` functions elsewhere in this crate.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::codebox` (7 passed); `cargo test -p compiler --test
codebox_backend` (40 passed); `cargo clippy -p codegen --lib` clean.

## Factor out duplicated boilerplate in the Rust backend emitter

Commit date: 2026-08-05

Source commit: this commit

Reviewed `backends/rust/mod.rs` for documentation gaps as the next step of the
doc-only sweep and found it already fully documented, with no gaps to fill —
the first backend in this sweep where that was true. Asked to look for
factorization value instead, and found two genuine cases of mechanical
duplication (as opposed to the rest of the file, where each match arm carries
distinct emission logic and isn't a good factoring candidate).

`emit_faust_dsp_trait_impl` emitted 15 near-identical `writeln!` calls, each
forwarding one `FaustDsp` trait method to its identically-named inherent
method with a fixed signature. Replaced with a `&[&str]` table of the 15
forwarding declarations and a loop, mirroring the table-driven style already
used for the And/Or/Xor operator tokens just below in the same file.

`emit_binop_expr`'s float-path `Add`/`Sub`/`Mul`/`Div`/`Rem` arms were five
near-identical arms differing only by operator token; merged into one arm
with a token lookup, the same shape as the And/Or/Xor arm right below it and
the same pattern used to merge Cranelift's `lower_binop` float arms earlier
in this sweep. Net change: -50 lines, no behavior change.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib`
(333 passed, unchanged from before — including
`lifecycle_conformance_matches_cpp_reference_order` and
`integer_state_arithmetic_uses_wrapping_semantics`, which would catch any
emitted-text regression from either refactor); `cargo clippy -p codegen
--lib` clean.

## Document the wasm backend

Commit date: 2026-08-05

Source commit: this commit

Reviewed the two non-test source files of the WASM backend (`layout.rs`,
`mod.rs`; ~5,500 lines) for documentation gaps, continuing the doc-only sweep
from Cranelift and interp. Already thoroughly documented overall; filled five
targeted gaps: why `Stack`-declared array locals need a linear-memory offset
in `layout.rs` (WASM's native `local` slots are scalar-only, unlike a native
stack); the four `SOUNDFILE_*_OFFSET` constants in `mod.rs`, which encode the
`Soundfile` struct at WASM's 4-byte pointer spacing versus the 8-byte spacing
used by native 64-bit backends for the same struct; a missing doc comment on
the recursive `lower_switch_cases` if/else-chain emitter; why `count` and
`sample_rate` safely share WASM local index 1 (they never coexist in the same
function body); and a step-by-step comment on `LoadSoundfileBuffer` lowering,
mirroring the already-commented Cranelift equivalent for a computation that's
harder to follow on WASM's implicit operand stack than with named SSA values.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::wasm` (38 passed); `cargo clippy -p codegen --lib` clean.

## Add small-format `.fbc` read support to the interpreter serializer

Commit date: 2026-08-05

Source commit: this commit

`write_fbc` has always supported two `.fbc` text formats (normal and compact
"small"), matching the C++ interpreter and the original porting plan
(`porting/phases/faust-rust-interpreter-porting-en.md`: "Two modes exist in
C++: normal and small. The Rust port must support both."), but `read_fbc`
only ever implemented the normal-format grammar. Small-format output was
unreadable by the Rust reader; every production `write_fbc` call site already
passed `small=false`, so the gap was latent rather than user-visible.

Auditing the reader showed its block/instruction-level parsing (meta, UI,
code blocks) never validates header keywords by literal text — it discards
label tokens positionally — so the two formats already aligned token-for-token
there. Only the top-level file header (7 lines, previously gated by
`check_token` against normal-mode-only keywords) and one literal `label ==
"name"` check on the optional per-instruction name field needed a small-mode
branch. `read_fbc` now detects the format from the first header token
(`"interpreter_dsp_factory"` vs `"i"`) and reads the matching header grammar;
the name-field check became positional so it accepts both `"name"` and `"n"`.

Confirmed `interp-ffi::read_fbc_any`'s precision auto-detection (which only
checks whether the header line ends in `"double"`) already worked unmodified
for both formats, and that no `.fbc` fixtures in the tree depend on the
small format.

Validation: `cargo build -p codegen -p interp-ffi --lib`; `cargo test -p
codegen --lib` (333 passed, up from 331 — two new tests: a full small-format
write/read round trip, and a `name`-field round trip exercised in both
formats, previously untested in either); `cargo clippy -p codegen --lib`
clean.

## Document the interp backend

Commit date: 2026-08-05

Source commit: this commit

Reviewed all 13 non-test source files of the interpreter (`interp`) backend
(~12,700 lines: `bytecode.rs`, `compiler.rs`, `executor.rs`, `factory.rs`,
`fbc_to_cpp.rs`, `foreign.rs`, `instance.rs`, `mod.rs`, `opcode.rs`,
`optimizer.rs`, `real.rs`, `serial.rs`, `soundfile.rs`) for documentation
gaps, continuing the doc-only sweep started on the Cranelift backend. This
backend turned out to be the most thoroughly documented in the crate: every
public item and most private helpers already carry a doc comment with precise
C++ source provenance, including exact line numbers in several places
(`instance.rs`). 11 of 13 files needed no changes.

Added one genuine gap fill: `foreign::is_supported_signature`'s shape
restriction (0-2 args, homogeneous type, matching the return type unless
`Void`) previously had no rationale attached. It mirrors the finite,
hand-enumerated set of `extern "C" fn` signatures that `foreign_call::invoke`
dispatches to via `transmute` in a separate crate — there is no generic FFI
path, so any signature outside that fixed table cannot actually be invoked at
runtime regardless of what the interpreter compiler accepts.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::interp` (164 passed); `cargo clippy -p codegen --lib` clean.

## Add Cranelift math-symbol coverage tests and polish backend documentation

Commit date: 2026-08-05

Source commit: this commit

Added exhaustive white-box unit tests for the Cranelift backend's four pure
`FirMathOp` -> host-symbol lookup tables (`unary_math_symbol_f32/f64`,
`binary_math_symbol_f32/f64`) in `lowering.rs`. The test's expected-value
match has no wildcard arm, so adding a new `FirMathOp` variant fails to
compile here instead of silently falling through the `_ => None` arms in the
production tables; the guarantee was confirmed by temporarily adding a
fictitious variant and reverting.

Reviewed all non-test source files in the Cranelift backend (`api.rs`,
`core.rs`, `host.rs`, `jit_data.rs`, `subset.rs`, `lowering.rs`) for
documentation gaps. The backend was already extensively documented; filled
the handful of genuine gaps found: `find_module_and_compute` and
`fir_type_to_clif_type` in `core.rs`; the non-obvious `exp10`/`abs`/`rint`/
`remainder` host wrapper semantics in `host.rs`; the generic
`function_body_subset_gap_reason_from_decl` engine in `subset.rs`; and the
silent element-type-mismatch behavior of static-table serialization in
`jit_data.rs`.

Merged the two near-identical `FirType::Float32 | FirType::FaustFloat` and
`FirType::Float64` match arms in `ComputeLowering::lower_binop` into one,
since Cranelift's `fadd`/`fsub`/`fmul`/`fdiv`/`fcmp` are width-agnostic and
operate on whatever CLIF value type the already-coerced operands carry. Pure
refactor, no behavior change.

Validation: `cargo build -p codegen --lib`; `cargo test -p codegen --lib
backends::cranelift` (32 passed); `cargo clippy -p codegen --lib` clean for
the touched module.
