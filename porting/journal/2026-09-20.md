# 2026-09-20

## The DDSP tutorial's introduction promised four parameters and stopped there, naming none of the second half

Commit date: 2026-09-20

Source commit: this commit

The opening paragraph of `libraries/optimizers-ddsp-tutorial-{en,fr}.md`
listed what a reader would have written by the end — a gain, a filter
pole, a resonant filter's frequency and Q, the five coefficients of a
biquad — and that list stopped at section 6. Everything the tutorial
grew since was invisible from its first screen: the loss you write
yourself, the hygiene of a schedule, a gate, a readout and a reset,
Newton where nothing has to be learned, `rad` in the graph and handed to
a host, the echo canceller and the small neural network, `ondemand` and
its spectral loss once per frame against a gradient at audio rate, and
the whole chapter on a wrong start (estimate, multistart, restart, a
loss that widens the basin, descent with no gradient). A reader deciding
whether the document answers their question was reading a table of
contents for a third of it.

The paragraph is now two: the first keeps the promise of concrete
programs and extends its list to the biquad's reflection coefficients,
the sixteen FIR taps from one reverse sweep, the echo canceller and the
neural network; the second names the themes in the order the tutorial
takes them and ends, as before, on the sentence that every program was
run on the current compiler and that the numbers follow each one. No
section was touched, and the claims were checked against the sections
they point at rather than written from the headings.

Both versions rewritten together; the structural checks still hold (39
headings and the same numbering sequence on each side, 78 code fences,
the code blocks identical once comments are stripped but for the four
that translate a filename or a label).

## "When the start is wrong" now comes before the closing sections of the DDSP tutorial, as section 12

Commit date: 2026-09-20

Source commit: this commit

`libraries/optimizers-ddsp-tutorial-{en,fr}.md` ended on three sections in
an order that sent the reader past the exit before the last teaching
chapter: 12 "Where to go next", 13 "Frequently hit walls", then 14 "When
the start is wrong", seven subsections on multistart, restarts, SPSA and
losses that widen a basin — the chapter a reader reaches the end of the
tutorial *needing*, placed after the pointers to other documents. It is
now section 12, directly after 11 (`ondemand`), so the body of the
tutorial ends with it and the two closing sections follow as 13 and 14.

No prose was rewritten; the block was moved whole and the numbering
follows it. Renumbered with it: the subsections 14.1–14.7 to 12.1–12.7,
the cross-reference "section 14 below" / "la section 14 ci-dessous" in
"Where to go next" to "section 12 above" / "la section 12 ci-dessus", and
the five references `(14.2) (14.4) (14.5) (14.6) (14.7)` in the symptom
table of "Frequently hit walls" to `(12.x)`. The two other references to a
"section 13" point at `docs/fad-rad-synthesis-{en,fr}.md`, not at this
file, and were left alone.

The two versions stay synchronized, checked rather than assumed: same
heading count (39) and same heading numbering sequence in both files,
same number of code fences (78), and the 39 code blocks identical once
comments are stripped except the four that translate a filename or a
label (`program.dsp`/`programme.dsp`, `lane`/`voie`,
`ondemand(body)`/`ondemand(corps)`), as before the move. The figures
quoted per section match one for one between the two files. Each file
changed by the same 128 lines.

## `-I DIR` could not override an installed library in `faustprobe` and the FFI factories, and `library()` ranked the source's directory ahead of `-I` where `import()` did not

Commit date: 2026-09-20

Source commit: this commit

Found while validating faustlibraries PR 251 (`bit_reverse_shuffle` on
`route`): a copy of `analyzers.lib` with a deliberately wrong permutation,
handed to `faustprobe -I mutdir`, changed nothing in the output, and the
same `-I old` (the master library) did not either. The C++ compiler and
`faust-rs -I` both saw the mutation. So every `faustprobe -I
../../faustlibraries` in the faust-diff-* projects had been measuring
`/usr/local/share/faust`, and the validation of the PR had to go through
the C++ compiler.

Two causes, one per resolver:

- `collect_search_paths_for_file` in `crates/cranelift-ffi/src/factory.rs`
  (and its twin in `crates/interp-ffi/src/factory.rs`) started from
  `default_import_search_paths` (the source's directory, `FAUST_LIB_PATH`,
  `<exe>/../share/faust`, `/usr/local/share/faust`) and *appended* the
  `-I` dirs, which made them dead for any name the installed libraries
  define. `paths.rs` documents the C++ order (user dirs first) and the CLI
  applies it through `merge_import_search_paths`; the FFI constructors
  had rebuilt the list by hand. Both now call
  `merge_import_search_paths` (made `pub` for that).
- With the order fixed, `import("x.lib")` resolved a `-I` dir first, but
  `library("x.lib")` still found a same-named file next to the program
  first: `SourceContext::for_file_with_metadata` in the evaluator
  prepends the source's directory to the search paths it receives. The
  compiler already passes that directory in its C++ position (after the
  `-I` dirs, before the installed libraries), so the context now keeps
  it there when it is present and prepends it only when it is not
  (callers that pass raw dirs keep their behaviour). `faust -I` from
  outside the program's directory gives the same answer.

Tests: `crates/cranelift-ffi/tests/import_dir_probe.rs` compiles a program
against an `analyzers.lib` in a `-I` dir that defines a symbol the real one
does not, with the control that the same program fails without `-I`, and
checks that `-I` beats a copy next to the program;
`crates/interp-ffi/tests/import_dir.rs` does the first through
`createCInterpreterDSPFactoryFromFile`. With the release binary rebuilt,
the mutated `analyzers.lib` is seen through `-I`, and `faustprobe --ref`
against a render made with `-I old` proves the PR's `fft(256)` and
`bit_reverse_shuffle(512)` bit-identical to master's (the mutation control
fails, as it must).

## The reverse sweep of `rad` replayed a recursion held by the operand of `exp`, `sqrt` or `abs`, advancing the forward state once more per block; the sweep now reads tapes only, and refuses what it cannot

Commit date: 2026-09-20

Source commit: this commit

Found by faust-diff-amp, the DDSP guitar amp of Yeh et al. (2024) ported
next to faust-rs: the loss lane of `rad(loss, params)` over a
Wiener-Hammerstein block, a gated recurrent unit between two biquad
cascades, differed between block sizes from the first frame after a
block boundary (0.085 on a loss of 0.01 at block 1 or 64, identical when
one block covered the render), while the `fad` program with the same
loss was the same at every block size. A primal must not depend on the
block. Reduced with `faustprobe --out` at block 1 and block 4096 over
twelve witnesses: a recursion under `rad` whose body holds `exp`,
`exp10` or `sqrt` of an operand that is not stateless (here a phasor:
`x + exp(-x*x) * hp` with `x = sin(2 pi phasor)`), or `abs` of one,
while `tanh`, `sin`, `log`, `pow`, a division and plain products are
exact. The C++ emitted for the witness showed the cause in the reverse
loop: the phasor's whole body lowered again, `fRec1 = fRecCur1`
included, its value used by nothing. `propagate_bra_unary_math_adj`
loaded the operand for every rule, and `collect_tape_needed_values`
tapes the *output* of `exp`, `exp10` and `sqrt` (their derivative is a
multiple of it), not the operand; `load_bra_fwd_value`, finding no tape,
fell back to `lower_signal`, which in the reverse loop emits the
operand's computation, and the state update of any recursion in it. The
dead value was eliminated; the update was not; the forward state
advanced twice per sample of the block. `abs` had the mirror case: its
rule reads the operand (taped) and its own output (not taped, so
lowered again, recursion included).

The fix: for `exp`, `exp10` and `sqrt` the sweep loads the taped output
alone; for `abs` it rebuilds `|x|` from the taped operand. And
`load_bra_fwd_value` no longer falls back silently: a value that is
neither taped nor trivially re-evaluable (a leaf or a stateless
combinator of leaves, the same predicate the tape analysis uses) is
refused with `FRS-SFIR-0004` naming the signal, so a rule and the tape
analysis that disagree on an operand are an error, not a drifting
primal. The causal placement (the sweep inside the forward loop) keeps
lowering, as before, since there the horizon is the current sample.
Tests `a_rad_whose_exp_operand_holds_a_recursion_keeps_the_primal_across_blocks`
and `..._abs_...` in `rad_runtime.rs`: two blocks of 64 must give the
samples 64..128 of one block of 128 (before the fix the second block was
off by 0.02 on 0.34); with the operand lowered again as a mutation the
first fails at frame 64, and with the output loaded again for `abs` the
guard refuses the program. The guard also caught `debug_softclip_gradient_values`
(`abs(0.0 * float(lcg))`) before the `abs` rule was corrected: the
same defect on a noise source, silent until then.

What the amp project measured with the defect in place is remeasured
there: a truncation table that attributed to the block horizon what was
the drifting primal, and a calibration of 209 parameters whose loss was
that of a model no longer the program's after the first block. The
one-block gradients against finite differences were right all along (no
boundary is crossed), which is why the defect hid behind them.

## A slider's initial value reached a `-double` program as its `f32` rounding

Commit date: 2026-09-20

Source commit: this commit

Also found by faust-diff-amp: an online descent rendered by `faustprobe`
followed one trajectory with its learning-rate slider at its default and
another with `--set lr=0.02`, the default's own value. The runtime
descriptor of `cranelift-ffi` kept `control_defaults` as `f32`
(`let init = init as f32` before both the UI item and the default), and
`apply_control_defaults` widened that rounding into the `f64` zone:
`hslider("lr", 0.02, ...)` started at 0.019999999552965164, a `--set`
delivered 0.02. A descent whose step is that value, through a saturating
chain with bounded parameters, diverged from the other in a few hundred
steps. The defaults are now kept at the FIR's precision (`f64`) and
narrowed only for an `f32` zone; the UI item's fields stay `f32`, the
width of the host interface. Test
`a_slider_starts_from_the_double_its_source_wrote` in
`crates/cranelift-ffi/tests/host_loop_probe.rs`: `--double` on
`hslider("lr", 0.02, 0, 1, 0.0001)` prints `0.02`, and `--set lr=0.02`
prints the same bits.
