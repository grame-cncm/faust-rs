# 2026-07-28

## Complete semantic diagnostic guidance (G7)

Commit date: 2026-07-28

Source commit: `d4b7d9ab`

Completed phase G7 of
`porting/compiler-diagnostics-v2-analysis-and-improvement-plan-2026-07-28-en.md`.
Added deterministic near-name suggestions ranked by bounded
Damerau-Levenshtein distance over the scopes the evaluator already recorded, so
no suggestion can name an unreachable symbol. The compiler turns an
unambiguously closest candidate into an exact rename edit, kept
`MaybeIncorrect` because reachability is not intent; equally close candidates
produce no edit.

Gave failed `case` matches a rule/attempt trace built from the rules tree:
one frame per declared rule in written order plus the arguments the matcher
dispatched on, with pattern variables rendered as bare identifiers and no
evaluator environments exposed.

Made both duplicate-declaration diagnostics label every participating site. The
parser's redefinition report lost its multi-line message in favour of a typed
`declarations` fact and one label per clause; `ParserDiagnostic` gained typed
detail-code, related-site, fact, note, and help fields to carry it.

Closed an acceptance-parity gap: duplicated runtime UI addresses. C++ rejects
two input controls sharing a path and only warns for two bargraphs;
`faust-rs` accepted both. `ui::find_duplicate_control_paths` now checks the
grouped `UiProgram`, so rejection depends on the program rather than on whether
JSON is generated, and `DuplicatePathKind` reproduces the C++ split exactly.
Anonymous controls are excluded because C++ renames unlabeled widgets before
they can collide. Widget declarations are recorded at parse time, since widget
boxes are rebuilt during evaluation and box provenance cannot cross that
boundary. New stable code `FRS-UI-0001` and `CompilerError::UiLayout`.

Split the math-domain rule into three outcomes by passing each operation its
domain explicitly: a constant fully outside stays an error, an operand
straddling the boundary becomes `InferenceWarning::PotentialMathDomain`, and an
operand inside is silent. Warnings are opt-in through
`Compiler::with_semantic_warnings` and `--warn`, are deduplicated, go to
stderr in the selected format, and never change the exit status.

Normalized note order at the `DiagnosticBundle` boundary
(cause, rule, computed, suggested target, context) with a stable sort, so
producers stay free to add notes in any order. Added
`porting/compiler-diagnostics-v2-g7-semantic-guidance-2026-07-28-en.md`.

Public Rust enum and struct surfaces are `adapted`; stable diagnostic codes,
CLI exit status, generated code, and external C/C++ ABI are unchanged.

Validation: new `crates/compiler/tests/semantic_guidance.rs` (10 cases) and
`crates/ui/tests/core_api.rs` conflict cases;
a full 351-file corpus rescan confirming the new UI check rejects nothing the
C++ compiler accepts;
`cargo fmt --all`;
`RUSTDOCFLAGS="-D warnings" cargo doc -p parser -p eval -p compiler -p ui -p sigtype -p diagnostics -p interval --no-deps`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- code-graphs`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- error-model-check`;
`cargo run -p xtask -- cli-parser-check`;
`cargo run -p xtask -- cli-transcript-check`;
`git diff --check`.

Also re-recorded `tests/cli-transcripts`, which had drifted from the compiler
since G2: the `--error-format json` doc comment added there and the JSON `meta`
parity fix landed later were never regenerated, leaving 36 stale transcripts
before this phase. Fixed two broken Rustdoc intra-doc links found while denying
warnings on the touched crates.

## Make parser recovery diagnostics actionable (G6)

Commit date: 2026-07-28

Source commit: `3f42babc`

Completed phase G6 of
`porting/compiler-diagnostics-v2-analysis-and-improvement-plan-2026-07-28-en.md`.
Converted parser repair sequences into typed expected/unexpected tokens,
normalized grammar terminal names to Faust lexemes, and added conservative
suggestions. Unambiguous missing semicolons and closing delimiters now produce
machine-applicable insertion edits. Diagnostics label the likely previous
token or matching opening delimiter. Same-span parser cascades collapse into
one primary diagnostic while retaining the remaining recovery reports as
structured related evidence.

Replaced the evaluator's `Vec<String>` transport for
`component(...)`/`library(...)` parse failures with the original
`DiagnosticBundle`. The compiler preserves nested parser codes, labels,
source snapshots, facts, and fixes, then appends evaluator context.

Added the public, Rustdoc-documented `ImportCycleEdge` and extended
`SourceReaderError::ImportCycle` with a deterministic closed edge path.
Textual and structural import expansion retain ordered active stacks. Cycle
diagnostics expose a typed path and one import-site label per recoverable
edge. Added
`porting/compiler-diagnostics-v2-g6-parser-recovery-2026-07-28-en.md`.

Public Rust enum surfaces are `adapted`; stable diagnostic codes, CLI behavior,
Faust semantics, generated code, and external C/C++ ABI are unchanged.

Validation: focused parser/import/component tests;
`cargo fmt --all`;
`RUSTDOCFLAGS="-D warnings" cargo doc -p parser -p eval -p compiler --no-deps`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- code-graphs`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- error-model-check`;
`cargo run -p xtask -- cli-parser-check`;
`git diff --check`.

The broader workspace Rustdoc command still reports pre-existing warnings in
`interp-ffi` and `xtask`; all G6-touched crates pass with warnings denied.

## Preserve diagnostics through FIR and backend lowering (G5)

Commit date: 2026-07-28

Source commit: `06c9c3b3`

Completed phase G5 of
`porting/compiler-diagnostics-v2-analysis-and-improvement-plan-2026-07-28-en.md`.
Added the Rustdoc-documented `FirOrigins` side table, which records prepared
Signal ids and their Box derivations beside FIR without changing hash-consing.
Scalar and checked-vector lowering record producer anchors; a deterministic
graph pass carries them through materialization, scheduling placement,
lifecycle/module assembly, and parent statements.

Added a provenance-aware FIR scaffolding sweep mapping. The compiler remaps
origins into the mandatory canonical FIR store and retains them in
`FirCompileOutput`. Transform preparation/lowering errors can now carry an
offending Signal and resolved Box candidates. FIR verifier and backend
diagnostics join `FirId -> Signal -> Box` with parser occurrences and immutable
source maps.

Promoted FIR/backend-local codes from `fir_code=...` and
`codegen_code=...` note protocols to `detail_code` plus typed facts. Added a
typed backend failure classification: valid unsupported constructs are
`unsupported_feature`; malformed roots/sections and other invariants are
`compiler_bug` and ask for a reproducer without blaming Faust syntax. Backend
errors may retain an offending `FirId`; C++ unsupported-node errors do so, and
WASM uses the canonical module as a conservative trace anchor until its error
surface captures exact nodes.

Public Rust APIs are `adapted` or extended: `SignalFirOutput` and
`FirCompileOutput` gain provenance; `SignalCompileOutput` retains its
definition root/entrypoint; `SignalFirError`, `SignalPrepareError`, and
`BackendCodegenError` gain typed context. `CompilerError::Transform.error` is
boxed to keep the public aggregate error below Clippy's large-error threshold.
Stable top-level diagnostic codes,
CLI behavior, external ABI, FIR fingerprints, and generated code remain
unchanged. Added
`porting/compiler-diagnostics-v2-g5-fir-backend-provenance-2026-07-28-en.md`.

Validation: `cargo fmt --all`; focused `transform`/`codegen`/`compiler`
provenance and CLI tests; `cargo check --workspace --all-targets`;
`cargo doc -p fir -p transform -p codegen -p compiler --no-deps`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- code-graphs`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- error-model-check`;
`cargo run -p xtask -- cli-parser-check`;
`git diff --check`.

## Carry source provenance through Signal typing (G4)

Commit date: 2026-07-28

Source commit: `78b7b01a`

Completed phase G4 of
`porting/compiler-diagnostics-v2-analysis-and-improvement-plan-2026-07-28-en.md`.
Added `SignalOrigins`, an explicit ordered and deduplicated
`SigId -> BoxId` derivation table populated during Box propagation. The table
does not affect Signal hash-consing. `SignalCompileOutput` now retains it so
the compiler can join derived Signal failures with the occurrence-aware parser
provenance introduced in G3.

Added mapped forest cloning in `tlib` and a provenance-aware verified
signal-preparation entry point. Origins are remapped into the private staging
arena and inherited through recursion conversion, projection
canonicalization, promotion, simplification, recursion merging, delay
canonicalization, and retyping.

Replaced `InferenceError(String)` with Rustdoc-documented typed variants and
stable `InferenceRule` values. Delay bounds, soundfile-part ranges, table
construction, compile-time mathematical domains, clock-environment misuse,
recursive invariants, and malformed Signal graphs now retain offending nodes,
operands, inferred types/intervals, and required contracts. Boxed the typed
payload in `CompilerError::Type` to preserve a compact aggregate error.

Kept stable code `FRS-COMP-0004` but introduced the explicit
`type_inference` stage. JSON v2 now exposes rule-specific detail codes and
typed facts; raw Signal ids and expressions are debug-only. Source labels are
selected from Signal derivations and G3 Box occurrences. Added structural and
end-to-end coverage for hash-consed provenance, staging rewrites, soundfile
parts, delay intervals, invalid table generators, and compile-time modulo by
zero. Added
`porting/compiler-diagnostics-v2-g4-signal-provenance-2026-07-28-en.md`.

Public Rust APIs are `adapted`: `InferenceError` is now an enum,
`CompilerError::Type.error` is boxed, `SignalCompileOutput` gains provenance,
and the preparation crate gains a provenance-aware entry point. Signal
semantics, stable diagnostic codes, CLI acceptance, generated code, and
external C/C++ ABI surfaces are unchanged.

Validation: `cargo fmt --all`;
focused `sigtype`/`propagate`/`transform`/`compiler` tests;
published JSON schema validation;
`cargo doc -p tlib -p propagate -p sigtype -p transform -p compiler --no-deps`;
`cargo run -p xtask -- code-graphs`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- error-model-check`;
`cargo run -p xtask -- cli-parser-check`;
`git diff --check`.

## Track occurrence provenance through Box evaluation (G3)

Commit date: 2026-07-28

Source commit: `60aca899`

Completed phase G3 of
`porting/compiler-diagnostics-v2-analysis-and-improvement-plan-2026-07-28-en.md`.
Implemented the G0 hybrid occurrence model in parser-session state:
`BoxOriginId`, definition/use `BoxOrigin`, candidate-preserving
`BoxProvenance`, and exact `LocatedBox` selection. Kept `TreeArena`
hash-consing and the legacy last-write-wins property store unchanged.

Fixed definition origins to retain the identifier token observed before the
right-hand side changes the parser cursor. Structural import expansion now
reconstructs source/destination node mappings and copies imported occurrences
through cross-arena re-interning.

Changed compiler diagnostic ownership selection to follow the definition
reference graph from the configured entry point. Identical hash-consed nodes in
reachable and unreachable definitions therefore blame the reachable source
occurrence. Within the selected definition, unique parser occurrences produce
primary use/operator labels; the enclosing definition and entry-point call
remain typed secondary labels. Ambiguous or generated nodes retain a
conservative fallback.

Added structural tests for synthetic and real-parser duplicate occurrences,
imported provenance remapping, and an end-to-end reachable-occurrence
diagnostic. Added
`porting/compiler-diagnostics-v2-g3-box-provenance-2026-07-28-en.md` with API
mapping, selection rules, invariants, performance observations, and pass
result.

Reran the release provenance probe at 250,000 occurrences/4,096 semantic
nodes: dense sets used an estimated 1,146,904 bytes (1,704,625 ns build,
271,125 ns query); located occurrences used 2,000,000 bytes (370,167 ns build,
41,541 ns query). The warmed release parser corpus body completed in
approximately 0.41 seconds. Measurements are observational, not portable CI
thresholds.

Public Rust APIs are `adapted`; no C ABI, semantic IR layout, generated code,
or source acceptance changed.

Validation: `cargo fmt --all`;
focused parser/compiler diagnostic tests;
release diagnostics provenance probe;
warmed release parser corpus run;
`cargo doc -p parser -p compiler --no-deps`;
`cargo run -p xtask -- code-graphs`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- error-model-check`;
`cargo run -p xtask -- cli-parser-check`;
`git diff --check`.

## Add typed diagnostics v2 schema (G2)

Commit date: 2026-07-28

Source commit: `6d54d839`

Completed phase G2 of
`porting/compiler-diagnostics-v2-analysis-and-improvement-plan-2026-07-28-en.md`.
Added the Rustdoc-documented typed diagnostic vocabulary for categories, detail
codes, deterministic facts, semantic label roles, causal traces, related
diagnostics, applicability-graded text edits, and debug evidence. The machine
renderer serializes these fields directly and never recovers them by parsing
human note, help, or label text.

Following the project owner's explicit compatibility decision, removed JSON
v1 instead of carrying two diagnostic formats. `--error-format json` now emits
the sole versioned `schema_version: 2` envelope; no `json-v2` CLI value or v1
renderer remains. Stable `FRS-*` meanings, human output, exit status, compiler
acceptance, generated code, and external C/C++ and Wasm surfaces are unchanged.
Updated the G0/G1 documents to distinguish their historical baseline checks
from the active contract.

Published `docs/diagnostics-v2.schema.json` (Draft 2020-12) and
`docs/diagnostics-v2-example.json`. The envelope includes compiler/request
metadata, status, immutable source snapshots and hashes, typed diagnostics, and
standard/debug disclosure. File-backed source text remains omitted by default;
memory and virtual-library text is retained because it cannot be recovered
later.

Migrated initial producers so JSON consumers no longer parse prose for eval
scope and binding chains, source-import search evidence, Box node debug
identity, FIR verifier context, or backend codes. Added explicit eval
definition/call roles and import-site roles. Signal type failures are
classified as user-code diagnostics, execution-option failures as invalid
options, and backend failures as unsupported features.

Added structural coverage for all typed field families, exact source ranges,
debug filtering, clean stdout, JSON success/failure parity, unresolved-import
facts, and every negative corpus entry. Validated the published schema and
example with a Draft 2020-12 validator. Added
`porting/compiler-diagnostics-v2-g2-schema-2026-07-28-en.md` with the breaking
decision, API mapping, privacy policy, invariants, and pass result. Regenerated
the public API index.

Public Rust APIs are `adapted`: the existing diagnostic types gain additive
typed fields and builders, while canonical machine locations use G1
`SourceRange`. The CLI JSON payload intentionally changes shape and consumers
must require schema version 2. There are no ABI, IR-layout, or generated-code
changes.

Validation: `cargo fmt --all`;
focused diagnostics/compiler/CLI tests;
Draft 2020-12 schema/example validation;
`cargo doc -p diagnostics -p compiler --no-deps`;
`cargo run -p xtask -- code-graphs`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- error-model-check`;
`cargo run -p xtask -- cli-parser-check`;
`git diff --check`.

## Add immutable diagnostic source maps (G1)

Commit date: 2026-07-28

Source commit: `cc1a4a84`

Completed phase G1 of
`porting/compiler-diagnostics-v2-analysis-and-improvement-plan-2026-07-28-en.md`.
Added a Rustdoc-documented immutable source layer to `diagnostics`:
`SourceMap`, `SourceMapBuilder`, `SourceFile`, `SourceId`, canonical half-open
UTF-8 `SourceRange`, SHA-256 `ContentHash`, and distinct terminal/LSP position
types. The line index preserves exact text and recognizes LF, CRLF, and bare
CR. Terminal columns account for four-column tab stops, combining marks, and
Unicode display width; LSP columns count UTF-16 code units.

Kept `SourceSpan` as the public v1 compatibility surface and added explicit
round-trip conversion through the source map. `DiagnosticBundle` now carries
immutable snapshots without exposing them in the existing JSON renderer.
Parser entry points register memory, file, imported-file, and embedded
virtual-library sources in deterministic visitation order. Parse, eval,
propagate, and signal-type failures retain this map.

Changed human snippet lookup to prefer the compiled snapshot and fall back to
the filesystem only for legacy bundles. A regression test compiles a snapshot,
changes the backing file, and proves rendering still shows the compiled text.
The same test proves adding/removing the map leaves v1 JSON byte-for-byte
unchanged.

Added structural tests for tabs, combining characters, wide CJK text, an
astral emoji, CRLF/bare-CR/LF, multi-line ranges, invalid UTF-8 boundaries,
content hashes, all source kinds, and parser import registration. Added
`porting/compiler-diagnostics-v2-g1-source-map-2026-07-28-en.md` with the
coordinate contract, API mapping status, invariants, risks, and pass result.
Regenerated dependency graphs and the public API index. Cleaned two private
intra-doc links so the touched compiler Rustdoc builds without warnings.

Public Rust APIs are `adapted`: C++ mutable/global source state becomes
immutable compilation snapshots. `SourceSpan`, diagnostics codes, v1 JSON,
human output shape, CLI status, C/C++ ABI, IR layout, and generated code remain
compatible.

Validation: `cargo fmt --all`; diagnostics/parser/compiler focused tests;
`cargo doc -p diagnostics -p compiler --no-deps`;
`cargo run -p xtask -- code-graphs`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- error-model-check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`git diff --check`.

## Freeze compiler diagnostics v2 baseline (G0)

Commit date: 2026-07-28

Source commit: `b14a45b3`

Completed phase G0 of
`porting/compiler-diagnostics-v2-analysis-and-improvement-plan-2026-07-28-en.md`.
Added the diagnostics-v2 baseline with explicit manual and Rust-only coverage
matrices, a nine-class quality sample, frozen v1 compatibility contracts, and
the representation gate required before provenance enters production IR.

Added the Clap-based `xtask diagnostics-provenance-probe` developer command
and Rustdoc for its command-line arguments and probe entry point. The probe
reproduces the structural ambiguity caused by attaching one mutable source
property to a hash-consed `TreeId`: origins 11 and 29 share one semantic node,
and the second write replaces the first. Structural tests show that dense
origin sets retain all candidates while explicit `(TreeId, origin)` located
occurrences retain the selected occurrence.

Selected a hybrid representation for later phases: compact origin sets keyed
by semantic node, plus located occurrences at ambiguity-sensitive boundaries
and worklist entries. This keeps semantic hash-consing unchanged and does not
embed source paths or diagnostic policy in `tlib`. Production types remain
deferred until G1 introduces stable source and origin identifiers.

Recorded debug and release observations for 250,000 occurrences sharing 4,096
semantic nodes on arm64 macOS 12.7.6. The sample estimates 1,146,904 bytes for
dense origin sets and 2,000,000 bytes for located occurrences; timing remains
observational and is explicitly excluded from cross-machine CI thresholds.

This phase adds an isolated measurement tool, tests, and documentation. It
does not change compiler IR, diagnostics, CLI behavior, generated code, or
external APIs.

Validation: `cargo fmt --all`; three focused provenance tests;
debug and release provenance-probe runs;
`cargo run -p xtask -- cli-parser-check`;
`cargo run -p xtask -- error-model-check`;
`cargo run -p xtask -- structure-check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`git diff --check`.

## Plan compiler diagnostics v2 provenance and tooling

Commit date: 2026-07-28

Source commit: `9ef96def`

Added
`porting/compiler-diagnostics-v2-analysis-and-improvement-plan-2026-07-28-en.md`.
The analysis compares the current Rust diagnostic pipeline with the historical
C++ `errormsg` model and the official Faust error manual. It records the strong
existing eval/propagate behavior—ranges, definition/use labels, scope facts,
binding traces, semantic rules, computed values, and actionable help—while
identifying the sharp loss of source context in signal typing, signal-to-FIR,
FIR verification, and backend emission.

The central design finding is that one source span cannot identify every
occurrence of a hash-consed semantic node. The proposed v2 architecture
therefore combines an immutable compilation `SourceMap`, canonical byte
ranges, a compact derivation-origin graph, and an occurrence-aware IR
prototype. It replaces machine information hidden in note strings with typed
facts, label roles, traces, diagnostic categories, detail codes, related
diagnostics, and applicability-graded source edits.

The G0–G8 plan first freezes a quality/performance baseline, then introduces
source snapshots and a versioned schema before carrying provenance through
Box, Signal, FIR, and backend layers. Later phases improve parser/import
recovery, semantic guidance and warnings, human multi-label rendering, and
AI/IDE integration. Existing `FRS-*` meanings, v1 JSON, CLI status, external
APIs, and C++ semantic parity remain compatibility constraints.

Updated `porting/faust-rust-diagnostics-model-en.md` to identify the new
document as the implemented baseline's second-generation follow-up.

This is documentation and planning only; no compiler behavior changed.

Validation: official manual and pinned C++ source review; representative
eval, propagate, type, and signal-to-FIR diagnostic inspection;
`git diff --check`.

## Enforce error and diagnostics ownership (E5)

Commit date: 2026-07-28

Source commit: `01053558`

Completed phase E5 of
`porting/error-diagnostics-separation-analysis-and-porting-plan-2026-07-28-en.md`.
Added the `xtask error-model-check` architecture guard and wired it into the
Ubuntu CI structure step. The check reads Cargo metadata and workspace
manifests to require the `diagnostics` package and reject the legacy `errors`
package, dependency name, alias, or path. It scans active Rust sources for the
old import/conversion vocabulary and parser message classifier, requires the
borrowing conversion plus typed compiler source/bundle markers, and rejects
reintroduction of the removed diagnostics crate scaffold.

Documented the final ownership rule: phase/backend crates own typed
operational errors and causal chains, `diagnostics` owns the stable report
schema, and boundary renderers own human/JSON presentation and exit policy.
Recorded the intentionally retained bounded string errors in lexer inspection,
C string/argument decoding, and private compiler/FFI adapters, together with
the condition that would require promoting one to a local typed error.

Updated active porting architecture maps and the FIR verifier document to use
the implemented `diagnostics`/`FirVerifyReport` boundary. Regenerated code
graphs; no generated artifact changed. Historical dated reviews and porting
status snapshots retain their original `errors` references as historical
evidence.

This phase adds a development/CI policy command and documentation only. It
does not change compiler, CLI, C/C++, WebAssembly, diagnostic schema, or
generated-code behavior.

Validation: `cargo fmt --all`; 70 `xtask` tests including four focused
error-model tests; `cargo run -p xtask -- error-model-check`;
`cargo run -p xtask -- cli-parser-check`;
`cargo run -p xtask -- ffi-boundary-check`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- code-graphs`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; active-document old-name audit;
`git diff --check`.

## Unify parser diagnostics at emission sites (E4)

Commit date: 2026-07-28

Source commit: `7aee00a5`

Implemented phase E4 of
`porting/error-diagnostics-separation-analysis-and-porting-plan-2026-07-28-en.md`.
Removed the parser-local `DiagnosticSeverity` enum and now store the canonical
`diagnostics::Severity`. Pending parser records require a `DiagnosticCode`;
default errors receive `PARSE_UNEXPECTED_TOKEN`, warnings and remarks receive
`PARSE_RECOVERY`, and lexer/parser-engine failures retain their explicit
family codes.

Moved invalid integer, float, signed-number, and import-path literal
classification to their emission sites with `PARSE_INVALID_LITERAL`. Removed
`parser_code_for_message`, so message wording can no longer change a stable
code. Pending parser records and their accessor are now private; the public
parse result remains the shared `DiagnosticBundle`.

Added a structural regression test proving that generic text containing
"invalid literal" keeps the generic code while an explicitly classified
message with entirely different wording keeps `PARSE_INVALID_LITERAL`. The
test also covers canonical warning/remark severities, recovery codes, and
error counts. Updated parser architecture/status documentation and regenerated
the public API index.

The pending parser representation is an `adapted` internal Rust API. Diagnostic
ordering, counters, locations, severities, stable codes, human/JSON rendering,
CLI status, and external APIs remain 1:1.

Validation: `cargo fmt --all`; `cargo test -p parser --all-targets`;
parser/compiler all-target tests and diagnostic transcript tests;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- code-graphs`; obsolete-taxonomy audit;
`git diff --check`.

## Expose typed compiler error sources (E3)

Commit date: 2026-07-28

Source commit: `ffb48605`

Implemented phase E3 of
`porting/error-diagnostics-separation-analysis-and-porting-plan-2026-07-28-en.md`.
`CompilerError::source()` now exhaustively exposes every concrete phase and
backend cause. Import, eval, propagation, type inference, execution-option,
signal-to-FIR, and all codegen variants return their typed error; the
aggregate-only parse, FIR-verifier, and missing-root variants deliberately
return no source.

Renamed the inference pass's duplicate `sigtype::rules::TypeError` to
`InferenceError`, while retaining the distinct leaf-validation
`sigtype::ops::TypeError`. `CompilerError::Type` now stores and re-exports the
typed inference failure instead of flattening it into `Box<str>`, without
changing the diagnostic message.

Added the total `CompilerError::diagnostic_bundle()` accessor and migrated all
workspace callers. The old optional `diagnostics()` API remains as a
deprecated compatibility wrapper. Removed the CLI's unreachable unstructured
fallback renderer because every facade variant carries a bundle. One
exhaustive regression test covers the source/no-source classification of all
facade variants and confirms total bundle access. Regenerated the public API
index.

The causal chain and typed payload are additive/adapted Rust API behavior.
Display text, diagnostic codes and schemas, CLI output/status, C/C++ and
WebAssembly surfaces, and compiler semantics remain 1:1.

Validation: `cargo fmt --all`;
`cargo test -p sigtype -p normalize -p compiler --all-targets`;
exhaustive `CompilerError` source-classification test;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- code-graphs`; obsolete-accessor/type-name audit;
`git diff --check`.

## Borrow operational errors when building diagnostics (E2)

Commit date: 2026-07-28

Source commit: `373d5e66`

Implemented phase E2 of
`porting/error-diagnostics-separation-analysis-and-porting-plan-2026-07-28-en.md`.
Replaced the consuming `IntoDiagnostic::into_diagnostic(self)` contract with
`ToDiagnostic::to_diagnostic(&self)`. `EvalError` and `PropagateError` now
produce the same structured diagnostic while remaining available to compiler
enrichment and error-chain logic.

Removed the two compiler-side error clones that existed only because
conversion consumed the value. Structural regression tests now convert owned
eval and propagation errors, then verify that their typed values and display
messages remain usable. Updated the active diagnostics architecture
documentation and regenerated the public API index.

This is an `adapted` internal Rust API mapping: the method name and receiver
changed, with no compatibility shim in the pre-1.0 workspace. Diagnostic
severity, stage, stable code, message, notes, help, rendering, CLI exit status,
and external C/C++ and WebAssembly behavior remain 1:1.

Validation: `cargo fmt --all`;
`cargo test -p diagnostics -p eval -p propagate -p compiler --all-targets`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- code-graphs`; obsolete-trait audit;
`git diff --check`.

## Rename the errors crate to diagnostics (E1)

Commit date: 2026-07-28

Source commit: `bf454cb1`

Implemented phase E1 of
`porting/error-diagnostics-separation-analysis-and-porting-plan-2026-07-28-en.md`.
Renamed the workspace package and source directory from `errors` to
`diagnostics`, then migrated the four direct consumers (`parser`, `eval`,
`propagate`, and `compiler`) to the canonical import path. The compiler facade
continues to re-export the diagnostic types used by its public API, while its
private `diagnostics` module is now named `diagnostic_enrichment` to distinguish
compiler-specific enrichment from the shared report data model.

Removed the unused `CRATE_NAME` and `crate_id` scaffold APIs. `Diagnostic` and
`DiagnosticBundle` remain structured report data and deliberately do not
implement `std::error::Error`; operational failures remain owned by the phase
crates. Regenerated the workspace dependency graphs and public API index after
the physical package rename.

The diagnostic data API is a 1:1 mapping. The Cargo package and Rust import
paths are an intentional pre-1.0 `adapted` rename without a compatibility shim,
as decided in E0. Stable codes, renderer output, CLI behavior, C/C++ and
WebAssembly surfaces, and compiler semantics are unchanged.

Validation: `cargo fmt --all`;
`cargo test -p diagnostics -p parser -p eval -p propagate -p compiler --all-targets`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- code-graphs`; Cargo metadata package-name audit;
active-source old-import audit; `git diff --check`.

## Freeze error and diagnostics separation contracts (E0)

Commit date: 2026-07-28

Source commit: `8a909843`

Added
`porting/error-diagnostics-separation-baseline-2026-07-28-en.md` to freeze the
contracts required before implementing the error/diagnostics separation plan.

The baseline inventories the four workspace consumers, records all 34 active
`FRS-*` codes and their existing enforcement tests, maps representative
human/JSON/process transcript gates, lists public diagnostic data surfaces,
and defines the expected `CompilerError::source()` result for every variant.
It also freezes parser recovery, ordering, location, severity, and code
behavior before removing the parser's parallel diagnostic representation.

No repository evidence identifies a supported external Cargo consumer or
publication workflow for the generic `errors` package. Phase E1 will therefore
use the plan's recommended direct pre-1.0 rename to `diagnostics`, without a
permanent compatibility shim. The Rust import path is an intentional adapted
API change; CLI, C/C++, WebAssembly, stable-code, rendering, and compiler
behavior remain unchanged. The compiler facade will re-export diagnostic types
used by its public API.

This phase is documentation only.

Validation: `cargo fmt --all`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; `git diff --check`.

## Analyze error and diagnostics ownership

Commit date: 2026-07-28

Source commit: `98496b4a`

Added
`porting/error-diagnostics-separation-analysis-and-porting-plan-2026-07-28-en.md`
in response to the review concern that the `errors` crate does not contain
idiomatic Rust errors.

The audit distinguishes phase-local operational errors from structured
diagnostic reports. `EvalError`, `PropagateError`, `SignalFirError`, backend
errors, and `CompilerError` already use typed `Result`/`Display`/`Error`
surfaces; `crates/errors` instead owns severity, stages, stable codes, labels,
notes, help, and bundles. The crate name is therefore misleading, while making
`Diagnostic` or `DiagnosticBundle` implement `std::error::Error` would be the
wrong correction.

The proposed E0-E5 migration freezes compatibility contracts, renames
`errors` to `diagnostics`, removes unused scaffold identity APIs, changes
phase-to-diagnostic conversion to borrow errors, restores
`CompilerError::source()` chains and typed signal-type failures, removes the
parser's duplicate severity/message-classification model, and adds narrow
architecture guardrails. The plan preserves `FRS-*` codes, human/JSON output,
CLI status, C ABI, and compiler behavior. It records the Rust package rename as
an `adapted` public API change and requires an external-consumer decision
before implementation.

This commit is documentation and planning only; it does not change runtime or
public API behavior.

Validation: `cargo fmt --all`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; `git diff --check`.

## Enforce CLI parser ownership in CI (C4)

Commit date: 2026-07-28

Source commit: `52e6474b`

Implemented phase C4 of
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`.
Added `cargo run -p xtask -- cli-parser-check`, which derives the workspace
package and binary/example inventory from Cargo metadata and scans package Rust
sources for unclassified process-argument consumers, handwritten
`parse_*_options` functions, and characteristic manual CLI diagnostics.

The check records the three approved legacy-token normalization layers that
immediately hand off to Clap: the compiler runner, Interpreter impulse runner,
and Cranelift impulse runner. It also keeps the two deliberately distinct
interfaces narrow and explicit: `ffi-common` parses an embedded C
`argc`/`argv` protocol, while the generated dependency-free Rust impulse
architecture remains the sole standalone process-parser exception. Unit tests
cover direct-parser and diagnostic detection, multiline xtask parser
detection, and a suffix-related false positive.

Added the checker to the Ubuntu structure CI gate and documented CLI parser
ownership and exception policy in `crates/xtask/README.md`. Successful
user-facing forms remain a 1:1 mapping through the preceding migrations;
Clap's status-2 invalid-invocation behavior remains the documented adapted
mapping. The embedded FFI protocol and standalone architecture behavior are
unchanged.

Validation: `cargo fmt --all`; `cargo test -p xtask cli_parser_check`;
generated `xtask --help` smoke; `cargo run -p xtask -- cli-parser-check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; `git diff --check`.

## Migrate secondary tools and examples to Clap (C3)

Commit date: 2026-07-28

Source commit: `98302412`

Implemented phase C3 of
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`
for `treearena_bench`, `interp_baseline`, `count_vector_corpus`,
`corpus_scan_cranelift`, `compute_bench`, and `export_clif`.

Each retained target now declares a typed Clap parser and generated help.
Paths use `PathBuf`; built-in benchmark fixtures and vector precision use
typed value enums; vector loop/scheduling ranges and shard syntax are
validated before compiler work; mutually exclusive DSP/fixture/FBC inputs are
declared as argument groups; free corpus filters remain repeatable and accept
leading hyphens. `export_clif` no longer panics for missing/extra CLI paths and
preserves Unix path bytes when constructing the C API strings.

Successful documented forms are a 1:1 mapping, including underscore fixture
names, optional positional `lv ss`, `--precision=...`, `--filter=...`,
`--shard=INDEX/COUNT`, the two `treearena_bench` argument orders, and free
Cranelift corpus filters. Invalid invocations intentionally use the C0 Clap
status-2 adaptation. The dependency-free generated impulse architecture
remains the sole process-argument exception and was not given a Cargo
dependency.

Added parser-definition and representative default/order/conflict/range tests
to all six targets. Direct process-argument access under `crates/` is now
limited to the compiler and two runner normalization layers that immediately
feed Clap.

Validation: `cargo fmt --all`; generated `--help` smoke for all six targets;
short executions of `treearena_bench`, `interp_baseline`,
`count_vector_corpus`, and `corpus_scan_cranelift`;
targeted all-target tests for the four owning crates;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; `git diff --check`.

## Replace the xtask parser tree with Clap (C2)

Commit date: 2026-07-28

Source commit: `877b0037`

Implemented phase C2 of
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`.
`xtask` now has one typed `XtaskCli`/`XtaskCommand` tree covering all 35
maintenance workflows. Typed argument groups declare paths, trace scenarios,
lane aliases, positive block counts, mutually exclusive corpus selections,
repeatable cases, release/debug switches, and workflow defaults. Workflow
modules receive validated option values; their handwritten dispatcher and
command-local token loops were removed.

`golden-gen-cpp` retains an `OsString` trailing-argument field that only starts
after `--`. Tests cover leading-hyphen values and a non-UTF-8 argument on Unix,
so forwarding to the reference compiler remains a 1:1 compatibility mapping.
Successful CI/documented commands and defaults remain 1:1. Unknown commands,
missing subcommands, conflicts, invalid enum values, and invalid numeric
values now follow the intentional C0 Clap status-2 adaptation; generated
help/version exits with status 0.

The typed dispatcher parses on the process main thread, then runs the selected
workflow on an explicit 64 MiB worker stack. This closed a stack overflow
observed while executing the complete `vector-interp-opt-check` matrix; all 40
checked-vector optimizer comparisons now complete.

Updated `crates/xtask/README.md` to use generated top-level/per-command help
and list the two existing CLI transcript commands. Added Clap definition,
active CI command-name, conflict/default/alias, repeated-case, unknown-command,
and passthrough tests.

Validation: `cargo fmt --all`; `cargo test -p xtask` (63 tests);
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- ffi-boundary-check`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- backend-align-smoke --skip-golden`;
`cargo run -p xtask -- vector-interp-opt-check`; help/error exit-status
smokes; `git diff --check`.

## Migrate the impulse runners to Clap (C1)

Commit date: 2026-07-28

Source commit: `c2a850c4`

Implemented phase C1 of
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`.
The Interpreter and Cranelift impulse runners now declare typed Clap parsers
for their DSP path, precision, frame count, import directories, vector
configuration, and scheduling strategy. A narrow `OsString` normalization
layer retains the Faust-compatible `-double`, `-single`, `-vec`, `-vs`,
`-lv`, and `-ss` spellings; `-I`, `-n`, and
`--scheduling-strategy` remain directly accepted.

Moved Clap 4 with derive support into the workspace dependency table and
reused it from the compiler and both runners. Parsing now occurs before the
large-stack worker is spawned, so Clap help/version and syntax diagnostics
retain their own exit codes. Compilation and runtime failures still use the
runner-specific status-1 diagnostic path.

Successful runner invocations are a 1:1 compatibility mapping. Invalid
command lines are intentionally adapted to the C0 contract: missing/invalid
arguments return status 2, while `--help` and `--version` return status 0.
The FFI factory's embedded compiler argument vector is unchanged; Cranelift
reconstructs the same canonical Faust flags after typed validation.

Added parser-definition checks, legacy-order and last-precision-wins coverage,
typed scheduling validation, and retained the existing runtime tests. Both
runners still produce the exact C0 eight-frame passthrough text with legacy
options placed on either side of the DSP path.

Validation: `cargo fmt --all`; targeted runner tests and executable help/error
smokes; exact Interpreter/Cranelift passthrough comparison;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; `git diff --check`.

## Freeze the CLI parser migration baseline (C0)

Commit date: 2026-07-28

Source commit: `907fa4bf`

Implemented phase C0 of
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`
by adding `porting/cli-parser-baseline-2026-07-28-en.md`.

The baseline classifies every direct process-argument consumer, inventories
active CI, Makefile, README, test, and generated-provenance invocations, and
freezes the successful command names, runner legacy flags, option repetition,
defaults, and C++ Faust trailing-argument contract. Embedded FFI argument
vectors, data formats, and the standalone dependency-free impulse architecture
remain explicit non-CLI exceptions.

Accepted the proposed invalid-invocation adaptation: Clap syntax failures will
return status 2, help/version status 0, and unknown `xtask` commands will no
longer print usage and succeed. No repository automation depends on the old
unknown-command status.

Recorded a byte-exact eight-frame passthrough baseline shared by the
Interpreter and Cranelift impulse runners.

Validation: direct execution of the current unknown-command and missing-DSP
paths; byte-exact execution of both runners on
`tests/corpus/rep_01_passthrough.dsp -n 8`; `cargo fmt --all -- --check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; `git diff --check`.

## Plan consolidation of custom CLI parsers

Commit date: 2026-07-28

Source commit: `0054dcaf`

Added
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`
in response to the code-review concern about multiple custom CLI argument
parsers.

The analysis confirms that the production compiler already follows the desired
legacy-normalization-then-Clap model, while the main debt is concentrated in
the two impulse runners, the `xtask` dispatcher and its command-local parsers,
and several developer binaries/examples. It separately classifies FFI
`argc`/`argv`, already-received compiler options, data-format parsers, and the
dependency-free impulse architecture as non-process-CLI protocols that must
not be migrated blindly.

The C0-C4 plan covers semantic baselines, runner migration, a typed Clap
subcommand tree for `xtask`, secondary tools, and a CI policy check. It
preserves successful and legacy Faust invocations while proposing standard
Clap diagnostics and exit status for invalid command lines. The public mapping
is 1:1 for successful workflows, adapted for invalid CLI behavior, and
unchanged for the FFI argument protocol.

Documentation validation: reviewed all direct `std::env::args` consumers and
known `xtask` option loops; verified repository-relative paths and
`git diff --check`.
