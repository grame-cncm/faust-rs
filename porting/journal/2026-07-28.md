# 2026-07-28

## Migrate secondary tools and examples to Clap (C3)

Commit date: 2026-07-28

Source commit: `98302412`

Implemented phase C3 of
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`
for `treearena_bench`, `interp_baseline`, `count_vector_corpus`,
`corpus_scan_cranelift`, `compute_bench`, and `export_clif`.

Each retained target now declares a typed Clap parser and generated help.
Paths use `PathBuf`; built-in benchmark fixtures and vector precision use
typed value enums; vector loop/scheduling ranges and shard syntax are
validated before compiler work; mutually exclusive DSP/fixture/FBC inputs are
declared as argument groups; free corpus filters remain repeatable and accept
leading hyphens. `export_clif` no longer panics for missing/extra CLI paths and
preserves Unix path bytes when constructing the C API strings.

Successful documented forms are a 1:1 mapping, including underscore fixture
names, optional positional `lv ss`, `--precision=...`, `--filter=...`,
`--shard=INDEX/COUNT`, the two `treearena_bench` argument orders, and free
Cranelift corpus filters. Invalid invocations intentionally use the C0 Clap
status-2 adaptation. The dependency-free generated impulse architecture
remains the sole process-argument exception and was not given a Cargo
dependency.

Added parser-definition and representative default/order/conflict/range tests
to all six targets. Direct process-argument access under `crates/` is now
limited to the compiler and two runner normalization layers that immediately
feed Clap.

Validation: `cargo fmt --all`; generated `--help` smoke for all six targets;
short executions of `treearena_bench`, `interp_baseline`,
`count_vector_corpus`, and `corpus_scan_cranelift`;
targeted all-target tests for the four owning crates;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; `git diff --check`.

## Replace the xtask parser tree with Clap (C2)

Commit date: 2026-07-28

Source commit: `877b0037`

Implemented phase C2 of
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`.
`xtask` now has one typed `XtaskCli`/`XtaskCommand` tree covering all 35
maintenance workflows. Typed argument groups declare paths, trace scenarios,
lane aliases, positive block counts, mutually exclusive corpus selections,
repeatable cases, release/debug switches, and workflow defaults. Workflow
modules receive validated option values; their handwritten dispatcher and
command-local token loops were removed.

`golden-gen-cpp` retains an `OsString` trailing-argument field that only starts
after `--`. Tests cover leading-hyphen values and a non-UTF-8 argument on Unix,
so forwarding to the reference compiler remains a 1:1 compatibility mapping.
Successful CI/documented commands and defaults remain 1:1. Unknown commands,
missing subcommands, conflicts, invalid enum values, and invalid numeric
values now follow the intentional C0 Clap status-2 adaptation; generated
help/version exits with status 0.

The typed dispatcher parses on the process main thread, then runs the selected
workflow on an explicit 64 MiB worker stack. This closed a stack overflow
observed while executing the complete `vector-interp-opt-check` matrix; all 40
checked-vector optimizer comparisons now complete.

Updated `crates/xtask/README.md` to use generated top-level/per-command help
and list the two existing CLI transcript commands. Added Clap definition,
active CI command-name, conflict/default/alias, repeated-case, unknown-command,
and passthrough tests.

Validation: `cargo fmt --all`; `cargo test -p xtask` (63 tests);
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`;
`cargo run -p xtask -- ffi-boundary-check`;
`cargo run -p xtask -- structure-check`;
`cargo run -p xtask -- backend-align-smoke --skip-golden`;
`cargo run -p xtask -- vector-interp-opt-check`; help/error exit-status
smokes; `git diff --check`.

## Migrate the impulse runners to Clap (C1)

Commit date: 2026-07-28

Source commit: `c2a850c4`

Implemented phase C1 of
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`.
The Interpreter and Cranelift impulse runners now declare typed Clap parsers
for their DSP path, precision, frame count, import directories, vector
configuration, and scheduling strategy. A narrow `OsString` normalization
layer retains the Faust-compatible `-double`, `-single`, `-vec`, `-vs`,
`-lv`, and `-ss` spellings; `-I`, `-n`, and
`--scheduling-strategy` remain directly accepted.

Moved Clap 4 with derive support into the workspace dependency table and
reused it from the compiler and both runners. Parsing now occurs before the
large-stack worker is spawned, so Clap help/version and syntax diagnostics
retain their own exit codes. Compilation and runtime failures still use the
runner-specific status-1 diagnostic path.

Successful runner invocations are a 1:1 compatibility mapping. Invalid
command lines are intentionally adapted to the C0 contract: missing/invalid
arguments return status 2, while `--help` and `--version` return status 0.
The FFI factory's embedded compiler argument vector is unchanged; Cranelift
reconstructs the same canonical Faust flags after typed validation.

Added parser-definition checks, legacy-order and last-precision-wins coverage,
typed scheduling validation, and retained the existing runtime tests. Both
runners still produce the exact C0 eight-frame passthrough text with legacy
options placed on either side of the DSP path.

Validation: `cargo fmt --all`; targeted runner tests and executable help/error
smokes; exact Interpreter/Cranelift passthrough comparison;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; `git diff --check`.

## Freeze the CLI parser migration baseline (C0)

Commit date: 2026-07-28

Source commit: `907fa4bf`

Implemented phase C0 of
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`
by adding `porting/cli-parser-baseline-2026-07-28-en.md`.

The baseline classifies every direct process-argument consumer, inventories
active CI, Makefile, README, test, and generated-provenance invocations, and
freezes the successful command names, runner legacy flags, option repetition,
defaults, and C++ Faust trailing-argument contract. Embedded FFI argument
vectors, data formats, and the standalone dependency-free impulse architecture
remain explicit non-CLI exceptions.

Accepted the proposed invalid-invocation adaptation: Clap syntax failures will
return status 2, help/version status 0, and unknown `xtask` commands will no
longer print usage and succeed. No repository automation depends on the old
unknown-command status.

Recorded a byte-exact eight-frame passthrough baseline shared by the
Interpreter and Cranelift impulse runners.

Validation: direct execution of the current unknown-command and missing-DSP
paths; byte-exact execution of both runners on
`tests/corpus/rep_01_passthrough.dsp -n 8`; `cargo fmt --all -- --check`;
`cargo clippy --workspace --all-targets -- -D warnings`;
`cargo test --workspace --all-targets`;
`cargo run -p xtask -- golden-check`; `git diff --check`.

## Plan consolidation of custom CLI parsers

Commit date: 2026-07-28

Source commit: `0054dcaf`

Added
`porting/cli-parser-consolidation-analysis-and-porting-plan-2026-07-28-en.md`
in response to the code-review concern about multiple custom CLI argument
parsers.

The analysis confirms that the production compiler already follows the desired
legacy-normalization-then-Clap model, while the main debt is concentrated in
the two impulse runners, the `xtask` dispatcher and its command-local parsers,
and several developer binaries/examples. It separately classifies FFI
`argc`/`argv`, already-received compiler options, data-format parsers, and the
dependency-free impulse architecture as non-process-CLI protocols that must
not be migrated blindly.

The C0-C4 plan covers semantic baselines, runner migration, a typed Clap
subcommand tree for `xtask`, secondary tools, and a CI policy check. It
preserves successful and legacy Faust invocations while proposing standard
Clap diagnostics and exit status for invalid command lines. The public mapping
is 1:1 for successful workflows, adapted for invalid CLI behavior, and
unchanged for the FFI argument protocol.

Documentation validation: reviewed all direct `std::env::args` consumers and
known `xtask` option loops; verified repository-relative paths and
`git diff --check`.
