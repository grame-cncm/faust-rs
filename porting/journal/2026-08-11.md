# 2026-08-11

## Close computed table-size and `tabulateNd` gap

Commit date: 2026-08-11
Source commit: this implementation commit

- Confirmed that the canonical global simplifier now reduces `tabulateNd`'s
  arithmetic table extent before FIR extraction; the real
  `basics.lib::tabulateNd_test` compiles in scalar/vector and runtime/const
  modes against the pinned library tree.
- Added self-contained `rep_87_table_computed_size`, a four-mode compiler test,
  Rust golden coverage, and the corpus-wide C++ shared-runtime differential.
- Regenerated the 220-case acceptance and backend reports: the new case is
  `OK/OK`, both C-family shells match, and all three numerical traces match.
- Made generated reports strip checkout-local paths and unstable C++ `clocked`
  pointer values so their versioned output remains portable and deterministic.
- Closed and removed `DIFF-GAP-002`; updated the live subset and historical gap
  notes to distinguish the former failure from current support.

## Add a corpus-wide Rust/C++ numerical differential

Commit date: 2026-08-11
Source commit: this implementation commit

- Added `xtask corpus-runtime-diff`, which classifies all 219 compile-corpus
  inputs, compiles the 102 mutually accepted DSPs to Rust and C++ interpreter
  bytecode, and executes both factories in the same Rust runtime under impulse,
  ramp, and sine inputs.
- Added append-compatible reading of C++ FBC version 8 while retaining version
  9 emission, with a serializer regression test.
- Recorded 95 matching cases (285 traces), four expected numerical mismatches,
  three explicit C++ Interp oracle limitations, 39 common rejections, and 78
  declared Rust FAD/RAD extensions; no unexpected result remains.
- Confirmed stream-wrapper `DIFF-GAP-001` numerically and registered
  `DIFF-GAP-014` through `DIFF-GAP-016` for wrapper-control, negative table
  index, and shifted variable-delay behavior.
- Added a fail-closed expectation file: new mismatches fail, oracle limitations
  remain visibly separate, and a known mismatch that starts matching is rejected
  as stale until its gap entry is removed.

## Replay compilation metadata in C-family callbacks

Commit date: 2026-08-11
Source commit: this implementation commit

- Added explicit non-identity metadata entries to the public C/C++ backend
  options (`adapted` API mapping) and replayed the combined stream in stable
  C++ key order, including `compile_options`, source identity, and declarations.
- Normalized imported metadata keys to their logical relative path or library
  basename instead of exposing canonical resolver paths.
- Matched C++ `eval.cpp` by inserting metadata wrappers from actually evaluated
  definitions into the explicit compilation-session store.
- Added a self-contained C/C++ callback test and a maintained differential over
  all eleven original gap cases plus `rep_40`; keys, values, and order match the
  pinned C++ compiler (the comparison ignores only the backend-specific
  `compile_options` value).
- Closed and removed `DIFF-GAP-013`.

## Match C++ C-family source identity metadata

Commit date: 2026-08-11
Source commit: this implementation commit

- Added explicit `metadata_name` and `metadata_filename` carriers to the public
  C and C++ backend option structs. This is an `adapted` API mapping: Rust keeps
  source identity explicit instead of reading C++ process-global state.
- The compiler facade now derives the callback filename from the source basename
  and the callback/banner name from top-level `declare name`, falling back to
  the source stem. Backend class-name overrides remain independent.
- Added C and C++ regression coverage for declared/default names, basename-only
  filenames, and a custom generated class name.
- Closed and removed `DIFF-GAP-012`; non-identity metadata transport remains
  tracked independently as `DIFF-GAP-013`.

## Match C++ ordering below synthesized UI roots

Commit date: 2026-08-11
Source commit: this implementation commit

- Changed `UiProgramBuilder` to retain ordering keys for root nodes and apply
  the same stable raw-label ordering used for children of explicit groups,
  matching C++ `UITree::prepareUserInterfaceTree()`.
- Added a structural root-order test and promoted `rep_38`, `rep_55`, `rep_57`,
  `rep_66`, and `rep_75` into the maintained C++ UI-event differential.
- Updated the Rust-only relative-group tests for the now-sorted `Bar`, `Foo`
  root order. A full 25-case UI audit now leaves only the two intentional group
  pathname extensions (`rep_63`, `rep_64`).
- Closed and removed `DIFF-GAP-011`; `DIFF-GAP-012` and `DIFF-GAP-013` remain
  open metadata parity defects.
- Reconciled the frozen diagnostic-code test table with the already documented
  `FRS-COMP-0006` const-table warning after the workspace gate exposed the
  omission.

## Audit corpus-wide differences against Faust C++

Commit date: 2026-08-11
Source commit: this documentation commit

- Audited all 219 `tests/corpus/*.dsp` inputs across acceptance/rejection,
  generated C/C++ shells, arity, metadata, and UI events. The current matrix is
  102 `OK/OK`, 39 `ERR/ERR`, 78 expected FAD/RAD extensions, and zero real
  acceptance divergence.
- Regenerated the Phase 4 status and Phase 6 full-backend reports for the
  current 219-case corpus. All 102 common cases retain matching C and C++ shell
  signatures and input/output arity.
- Added a dated detailed audit under `porting/` and registered the generated
  C/C++ source-text contract as adapted `DIFF-BACK-004`.
- Added `DIFF-GAP-011` for the root-sibling ordering defect exposed by
  `rep_38`, `rep_55`, `rep_57`, `rep_66`, and `rep_75`. It is explicitly a
  parity gap to close, not an intentional Rust extension.
- Added `DIFF-GAP-012` because all 102 common cases expose `mydsp` source
  identity instead of the DSP filename or explicit `declare name`, and
  `DIFF-GAP-013` because 11 cases lose non-identity master/import/library
  metadata in generated C/C++ `metadata()` callbacks.

## Add the consolidated Faust C++ difference registry

Commit date: 2026-08-11
Source commit: this documentation commit

- Added `porting/faust-rs-vs-faust-cpp-differences-en.md` as the living index
  of Rust extensions, adapted contracts, observable behavior differences,
  narrower surfaces, and frozen exclusions relative to the pinned C++ tree.
- Added a mandatory `AGENTS.md` maintenance rule so future parity-sensitive
  changes update the registry in the same change and link supporting evidence.
- Re-verified the UI pathname boundary against the official manual, the pinned
  C++ propagation code, and generated output. Registered relative pathname
  navigation in explicit group labels as Rust-only `DIFF-BEH-008`; terminal UI
  element labels retain C++ parity.

## Const table sample-rate contract

Commit date: 2026-08-11
Source commit: `3d97cb58`

- Added `--table-init-sample-rate HZ` for `--table-init const` generators that
  read `ma.SR`. Omitting it remains a typed transform failure; supplying it
  embeds that exact value in the table.
- Added opt-in diagnostic `FRS-COMP-0006`, emitted through `--warn`, to make
  the frozen value and the `--table-init runtime` alternative explicit.
- Verified both frozen siggen SR fixtures at 48 kHz and retained rejection of
  foreign-function generators.
